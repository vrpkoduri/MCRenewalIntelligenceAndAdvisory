# Sprint Tracker

Fast-moving operational log of sprint status, decisions, and test results. The formal
narrative roadmap is `Morgan_Cash_Build_Plan_Sprint_Roadmap.docx`; this is the living index (GENERAL_INSTRUCTIONS Rule 1).

## Status at a glance

| Sprint | Block | Status | Plan | Notes |
|---|---|---|---|---|
| **S0** | Ingestion → silver | **Complete** | [SPRINT_0_PLAN](sprints/SPRINT_0_PLAN.md) | Bronze **live** (4 SF objects, G1 ✅); **all 3 silver tables in PROD 2026-05-31** — `deals` (3,959), `offers` (57,586), `field_history` (220,172); every table reconciles to bronze, schema + no-surface green. Remaining: FU-001/FU-002 (deferred, non-blocking) |
| S1 | Identity + Deal table | **Complete — in PROD `gold` (2026-05-31)** | [SPRINT_1_PLAN](sprints/SPRINT_1_PLAN.md) | **D-101…D-105 signed; C-013 (PORT AATM) + C-014 (own `merchant_id` + carry `azure_merchant_id`).** `common/identity/{normalize,match,keys}` + `schemas/gold.py` + gold field maps + transforms `gold_{merchants,deals}.py`. **106 tier-1 green.** **Tier-2 recon PASSED on `gold_test` then PROMOTED TO PROD (`failures: []` both):** `gold.deals` **3,959**, `gold.merchants` **2,125**, `gold.merchant_crosswalk` **2,167**; 0 null merchant_id, collapse 1.0198, azure fill **81.4%**, gaps null+flagged, FU-002 Wolf=1. |
| S2 | Features + clock (Appendix A) | **Complete — in PROD `gold` (2026-05-31)** | [SPRINT_2_PLAN](sprints/SPRINT_2_PLAN.md) | THE-core-principle sprint: daily live-recompute of balance/paydown/eligible-date/closure; never trust SF stored balances. **Decisions:** separate point-in-time `gold.deal_clock`/`merchant_clock` (+`_current` views); estimated-path-only (no feed); plain M–F v1 (holiday hook); 55% default threshold (FU-201 to wire per-funder). Built `common/clock/{amortization,calendar,closure,rollup}` + `transform/gold_clock.py` + clock field maps/schemas. **165 tier-1 green** (+59 clock). **Tier-2 recon PASSED on `gold_test` then PROMOTED TO PROD (`failures: []` both, run_date 2026-05-31):** `gold.deal_clock` **3,959** (= gold.deals, 1 row/deal) + `gold.merchant_clock` **2,125** (= distinct merchants) + `*_current` views; closure closed_clean 3,059 / active 894 / **closed_default 6** (Starr default-note invariant held — 0 default-note-but-clean); paydown∈[0,1] + balance≥0 + balance_source 100% estimated + revenue/burden 100% null+flagged all clean. Diagnostics: clock_inputs_missing 234, rtr_checkpoint>tol 21 (FU-202), elapsed-at-term-cap 3,826 (run_date future vs real book). |
| S3 | Rung classifier + state machine + event log (Appendix B) | **Complete — in PROD `gold` (2026-06-02, `failures: []`)** | [SPRINT_3_PLAN](sprints/SPRINT_3_PLAN.md) | Two-stage deterministic engine: Step-0 lifecycle gate → 5-rung waterfall (first-match-wins + stress override), `{lifecycle_state, rung, confidence, missing_signals[], direction_of_travel}` per merchant; append-only event log; daily queue read. **No ML** (S6). Reads S2 clock only — never recomputes the spine. **BUILT 2026-06-02:** `common/rung/{lifecycle,waterfall,confidence,classify}.py` (pure) + `common/eventlog/events.py` (pure append-only) + `schemas/gold.merchant_rung_schema()`/`event_log_schema()` + `field_maps.MERCHANT_RUNG_MAP`/event-log columns + `constants` enums (`LifecycleState`/`RungState`/`DefaultSubtype`/`LifecycleRoute`/`DirectionOfTravel`/`EventType`) + `RAPID_REUP_MAX_GAP_DAYS=45` + `GoldTable.MERCHANT_RUNG`/`MERCHANT_RUNG_CURRENT`/`MERCHANT_EVENT_LOG` (reused existing `Thresholds`, no duplicate numbers — Rule 3). **52 new tier-1 (42 rung + 10 eventlog) → 217 tier-1 green.** **D-301…D-306 implemented literally (C-017):** v1 stress = default-note+burden>0.30+worsening-terms (NSF→FU-301); `rapid_reup_flag` owned in `common/rung` (prior paydown<50% & still active PRIMARY, ≤45-day gap FALLBACK, reuses S2 clock for paydown-at-date); trust `Type=Renewal` (renewal_chain_incomplete = data gap, FU-302, never a disqualifier); separate `gold.merchant_rung` + append-only `gold.merchant_event_log`; one wide event log keyed (merchant_id,event_type,event_ts); borderline-driven confidence ∈[0.5,1.0] (missing data never penalized). **NEXT (gated):** `transform/gold_rung.py` (Spark) + `gold_test` build/recon — **awaiting approval (Rule 5).** Exit: whole book classified, Unclassified pile quantified, reconciles, 4-merchant B.5 labels match. |
| S4 | Activation + Book Health | **Complete — in PROD `gold` (2026-06-02, `failures: []`)** | [SPRINT_4_PLAN](sprints/SPRINT_4_PLAN.md) | Put the intelligence in front of reps + management: **state machine** (`current_state`: clock-running/approaching/in-market/renewed/lost-winback) + **named plays + SLA + next actions**, a **daily queue**, a **read-only serving layer** (gold views; **D-403 — NO Salesforce write in S4**, governed SF write-back deferred to **FU-401**), and read-only **Book Health** (Framework 5.8 three views) over gold + event-log transitions. **No ML/offers/comms/app** (S5–S9). Reads S1/S2/S3 gold; never recomputes the spine. **D-401…D-406 signed 2026-06-02 (C-018):** states + `APPROACHING_WINDOW_DAYS=RENEWED_WINDOW_DAYS=30`; play priority matrix + SLA tiers 2/5/10 business days + `play_owner` from SF OwnerId (else null+flag); serving-layer-only reverse-ETL (FU-401 for the SF write); Book Health point-in-time family + v1/deferred metric split; S4 absorbs the state machine+plays; `daily_queue` view. New modules `common/activation` + `common/bookhealth`. |
| S5 | Offer Engine | **Decisions signed (C-019); offline pure modules BUILT & GREEN (270 tier-1). D-501 = batch handoff; MRI-side cloud build gated on FU-501 (handoff contract w/ routing team)** | [SPRINT_5_PLAN](sprints/SPRINT_5_PLAN.md) | **Reuse, not rebuild** (CLAUDE.md §6): the funder-criteria dataset + routing engine = the **`mca_funders`** catalog (silver boxes + gold `routing_program_evaluations`/`routing_decisions` + `v_funder_input`; engine code in `mca-funder-2A-3`/DevOps `DataManagement`). S5 = the integration layer: build an MRI merchant profile → reuse/invoke the engine → write `eligible_offer_types`/`matched_funders`/`max_sustainable_advance`/`best_offer_summary` to point-in-time `gold.merchant_offers`; **suitability gate** = the renewal-vs-buyout double-dip honesty check (serves Wolf); compliance = interface only (S8). **No ML/comms/app; no routing rebuild; no writes to `mca_funders`.** **Open D-501** reuse mechanism (invoke vs reuse-evaluations — headline, needs spike); **D-502** MRI↔funder id join (azure_merchant_id/tax_id, spike); **D-503** profile assembly + missing fields; **D-504** eligible_offer_types rules; **D-505** max_sustainable_advance (null+flag v1, revenue-dependent); **D-506** renewal-vs-buyout suitability gate (build); **D-507** `gold.merchant_offers` point-in-time; **D-508** compliance interface-only. |
| S6 | Prediction | Not started | — | |
| S7 | Agentic extraction (Statement Analyst + Data Steward) | Not started | — | agents extract only; never compute the spine |
| S8 | Advisory comms + agents (Composer, Structure Advisor) + compliance | Not started | — | compliance gate is first-class |
| S9+ | Merchant app | Not started | — | renderer over Lakebase |

## Sprint 5 — progress detail

**Decisions:** D-501…D-508 opened (SPRINT_5_PLAN §8). D-503…D-508 **approved 2026-06-02** ("approve all as recommended"). D-501 (reuse mechanism) + D-502 (id join) deferred to read-only spikes → now need a final call.

**Spikes (read-only, 2026-06-02 — Starter Warehouse, stopped after):**
- The funder-criteria dataset + routing engine = the **`mca_funders`** catalog: structured criteria boxes (`silver.funder_programs` **17 active** / `funder_program_versions` **94** / `funder_industries`/`funder_states`/`funder_operations`; **12 funders**), input contract `gold.v_funder_input`, routing outputs `gold.routing_program_evaluations`/`routing_decisions`. Engine code = workspace `mca-funder-2A-3` (criteria *extraction*) + the routing/matching engine (versioned: `routing_engine_version`, `box_version_id`).
- **DECISIVE (D-502):** the routing OUTPUTS cover only the **new-deal/submission** population — **121 distinct merchants**, 539 decisions, latest 2026-06-01. **MRI funded-book ↔ funder merchant_id overlap = 0** (different id spaces: MRI `azure_merchant_id` ≈ `1.0M`/`HIST-…` vs funder `3.4–5.2M`; `v_funder_input` carries no tax_id to bridge). ⇒ **reusing existing evaluations is NON-VIABLE for the funded book** — the engine must be RUN against MRI-built profiles. Mechanism choice (D-501) is now the headline open decision.

**Done (offline code, 2026-06-02 — no cloud writes; all mechanism-independent):**
- **`common/offer/` (pure, Spark-free-at-import — mirrors `common/rung`):**
  - `structure.py` — **renewal-vs-buyout (D-506 / Framework §5.7):** `double_dip_cost` (= est_current_balance × (factor−1), real numbers), `recommend_structure` (paydown < 50% → **wait-and-paydown**; multi-position → **buyout**; else **renewal** — the math decides, reuses `DISCIPLINED_RENEWAL_PAYDOWN_MIN`), `structure_evaluation`.
  - `suitability.py` — the gate (engine proposes / advisory disposes): `suitability_verdict` (surface / **suppress** a double-dip buyout / **wait**), `is_suitable`, + `compliance_gate_hook` (S8 interface only, D-508).
  - `offer_types.py` — `candidate_offer_types` (D-504): gated/unclassified → none-yet; serial/rapid-reup → buyout+larger-advance; in-market disciplined → renewal+larger-advance.
  - `profile.py` — `build_funder_profile` (D-503): MRI gold → the `v_funder_input` shape; populates state/FICO/TIB/positions/industry; **revenue/NSF/bankruptcy always missing in v1 (no feed) → flagged, never faked**; `tib_months` helper.
- **Schemas + maps:** `schemas/gold.merchant_offers_schema()`; `field_maps.MERCHANT_OFFERS_MAP` (+ `max_sustainable_advance_is_missing` / `offer_profile_unmatched` DQ) + `merchant_offers_columns()`; `matched_funders` verdict = **Reuse** (the existing engine, not rebuilt). `constants`: `OfferType`/`OfferStructure`/`SuitabilityVerdict` enums, `FunderCatalog` (the `mca_funders` fq names), `GoldTable.MERCHANT_OFFERS`/`_CURRENT`, `EventType.OFFER_COMPUTED` — reused existing `Thresholds` (no duplicate numbers, Rule 3).
- **Tests: 19 new tier-1 → 270 tier-1 green** (`tests/test_offer.py`). Cover the double-dip math + structure recommendation (Wolf → wait-and-paydown, $20,880 rollover cost), the suitability gate (suppress a double-dip buyout / wait), candidate offer-type rules, profile assembly with honest missing-field flags, and map/no-surface/Reuse invariants.

**D-501 SIGNED (C-019) → batch handoff:** MRI builds + lands `v_funder_input`-shaped profiles for the funded book; the **existing routing engine evaluates them on a cadence** (routing side owns it — no engine rebuild/port); MRI **reads results back** and maps to `gold.merchant_offers`. Because the id-overlap is 0 (D-502), the handoff must carry **MRI `merchant_id` through the run** (results keyed back to MRI). **New follow-up FU-501:** define that handoff contract with the routing team (landing table + MRI-merchant_id round-trip + cadence) — **spec drafted: [FU-501_offer_handoff_contract.md](FU-501_offer_handoff_contract.md)** (ready for routing-team sign-off).

**MRI-side cloud build (gated on FU-501 + approval):** `transform/gold_offers.py` = (1) **profile export** (gold → the `v_funder_input` shape, landed for the routing batch) + (2) **results consumer** (read the routing results for MRI merchants → map to `eligible_offer_types`/`matched_funders`/`max_sustainable_advance`/`best_offer_summary` + apply the suitability gate → write point-in-time `gold.merchant_offers`). Both are codeable now against a stubbed handoff contract, but **end-to-end tier-2 needs the routing batch to have produced MRI results** (FU-501). **Offline S5 committed.**

## Sprint 4 — progress detail

**Decisions:** D-401…D-406 signed 2026-06-02 via **C-018**. D-403 resolved to the **serving-layer-only** path (NO Salesforce write in S4; governed SF write-back deferred to **FU-401**).

**Done (offline code, 2026-06-02 — no cloud touched):**
- **`common/activation/` (pure, Spark-free-at-import — mirrors `common/rung`):**
  - `state_machine.py` — `current_state` (D-401): gated lifecycle (defaulted/dormant → `lost-winback`, new-establishing → `clock-running`); active resolves **renewed** (funded within `RENEWED_WINDOW_DAYS=30`) → **in-market** (`is_eligible_now`) → **approaching** (eligible within `APPROACHING_WINDOW_DAYS=30`) → **clock-running**. `state_changed` drives the `state_transition` event.
  - `plays.py` — `active_play` (D-402) deterministic priority matrix (do-not-fund-review › win-back › new-establishing-nurture › distressed-stabilize › slide-intervention › serial-renewal-vs-buyout › growth-upsell › graduate-referral › review-unclassified › in-market-renewal / approaching-prep / disciplined-reinforce). `play_sla_due` **reuses `common.clock.calendar.nth_business_day_after`** (SLA tiers 2/5/10 business days — Rule 3, no new calendar). `play_owner` pass-through (SF OwnerId else null+flag). `next_tactical_action`/`next_strategic_nudge` = grounded per-play templates (internal rep guidance — NOT comms).
  - `activate_merchant` — the composed entry point → `{current_state, active_play, play_sla_due, play_owner, next_tactical_action, next_strategic_nudge}`.
- **`common/bookhealth/metrics.py` (pure):** None-safe `pct`/`ratio`/`net_drift`/`distribution` + the **metric registry** (Framework 5.8) marking **v1-available** (rung distribution + drift, default/restructure trend, sliding count, approaching pipeline, concentration risk, partial renewal-capture) vs **deferred** (aggregate-LTV, defection, offer-acceptance, value-to-ask, comms time-to-contact — inputs are S5/S6/S8; surfaced, never faked).
- **Schemas + maps:** `schemas/gold.py` `merchant_activation_schema()` + `book_health_schema()` (tall point-in-time); `field_maps.MERCHANT_ACTIVATION_MAP` (+`play_owner_is_missing` DQ), `BOOK_HEALTH_MAP`, and `SF_WRITEBACK_REFERENCE` (documentation of the FU-401 SF write-back — floor/dual-audience fields → dedicated `MRI__*` fields, never `_sf_stored_*`). `constants`: `CurrentState`/`Play`/`BookHealthView` enums, `APPROACHING_WINDOW_DAYS`/`RENEWED_WINDOW_DAYS=30`, `PLAY_SLA_BUSINESS_DAYS` tiers, `GoldTable.MERCHANT_ACTIVATION`/`_CURRENT`/`DAILY_QUEUE`/`BOOK_HEALTH`+`_current` family, extended `EventType` (`state_transition`, `play_fired`) — **reused existing `Thresholds`; no duplicate numbers** (Rule 3).
- **Tests: 29 new tier-1 → 246 tier-1 green** (`tests/test_activation.py` 21, `tests/test_bookhealth.py` 8). Cover: state machine for clock×rung×lifecycle incl. boundaries + the four merchants; play priority (distressed-beats-all, slide-before-posture, rung/timing plays, unclassified); SLA reuses the business-day calendar + every play has a tier; grounded next-actions present; composed output object; metric v1/deferred split + None-safe math; activation/book-health/SF-writeback **no-surface** + floor/dual-audience-only invariants.

**Done (cloud code, 2026-06-02 — approved for `gold_test`):** `transform/gold_activation.py` (applies the pure `activate_merchant` via UDF over per-merchant signals from `merchant_rung_current` + `merchant_clock_current` + `gold.deals`; writes point-in-time `gold.merchant_activation` (+`_current`, `replaceWhere` idempotent), the `gold.daily_queue` view (sliding-first ordering with `queue_rank`), and appends `state_transition`/`play_fired` events to the shared event log via delete-then-append on this run's S4 types — leaves S3 rows untouched) + `transform/gold_book_health.py` (tall scoreboard + 3 `_current` views). Extended `common/eventlog/events.py` (S4 builders + 4 nullable cols) + `EventType`. Tier-2 `tests/tier2/recon_activation.py` + `run_tier2_activation.py` (`second_run` widget).

**Tier-2 recon PASSED on `gold_test` (2026-06-02, run_date pinned, `failures: []`):** built `merchant_activation` + `daily_queue` + `book_health` (+ all `_current` views) via the same `build_gold_activation`/`build_gold_book_health` drivers (serverless, staging cleaned up). **Hard checks green** — coverage `merchant_activation`=**2,125** (= distinct merchants on `merchant_rung_current`, key unique); schemas == `merchant_activation_schema()`/`book_health_schema()`; **current_state ∈ enum / active_play ∈ enum / play_sla_due non-null all 100%**; **`play_owner_is_missing` 100% true** (no SF owner source in v1, FU-101 — never fabricated); no-surface clean on all three; **`daily_queue` covers the book (2,125) with a unique `queue_rank`**; **Book Health reconciles** (Σ rung_distribution = 2,125 = `merchant_rung_current`); the two v1 views (`book_health`, `leading_indicators`) populate while **`renewal_performance` is intentionally empty (every metric deferred to S5/S8)**; **two-run state machine (2nd run 2026-06-03): prior partition unchanged (append-only), the S3 classification events untouched, `_current` advanced**. **Diagnostics:** current_state **lost-winback 1,464 / clock-running 458 / in-market 192 / approaching 6 / renewed 5**; plays **win-back 1,458 / new-establishing-nurture 239 / serial-renewal-vs-buyout 169 / review-unclassified 141 / distressed-stabilize 65 / in-market-renewal 47 / do-not-fund-review 6** (sum 2,125); book_health 22 metric rows; run2 transitions 0 (identical 1-day-apart signals, expected). **Reference-by-name:** Wolf → `current_state=renewed` / `active_play=serial-renewal-vs-buyout` / SLA 2026-06-09 (B.5 match).

**PROD `gold` promotion DONE (2026-06-02, approved):** re-ran the same verified drivers with `schema=gold, allow_prod=True, second_run=False` (clean single `activation_run_date` partition — the two-run state-machine check was a `gold_test`-only artifact) — **recon identical (`failures: []`)**. PROD objects: `mca_mri.gold.merchant_activation` (managed, 2,125) + `merchant_activation_current` (view) + `daily_queue` (view) + `book_health` (managed, 22 metric rows) + `book_health_current`/`renewal_performance_current`/`leading_indicators_current` (views). Workspace staging cleaned up. **NO Salesforce write** (D-403 serving-layer-only).

**S4 exit criteria met + in PROD** (whole book activated 2,125/2,125; daily queue + Book Health populate + reconcile; state machine append integrity; Wolf B.5 label matches + tier-1 covers all four). Follow-ups (non-blocking): **FU-401** (governed Salesforce write-back to dedicated `MRI__*` fields, sandbox-first, `allow_sf_write`-gated, + Lakebase serving sync); the deferred Book Health metrics (LTV / defection / offer-acceptance / value-to-ask / comms-SLA) fold in with S5 (offers) / S6 (predictions) / S8 (comms). Daily activation DAB job defined-as-code; scheduling approval-gated. **S4 activation + Book Health path COMPLETE.**

## Sprint 3 — progress detail

**Decisions:** D-301…D-306 signed 2026-06-01 via **C-017** (DECISIONS log). No new decisions arose during the build — all six were implemented literally.

**Done (offline code, 2026-06-02) — pure modules + schemas + tier-1 (cloud NOT touched):**
- **`common/rung/` (pure, Spark-free-at-import — mirrors `common/clock`/`common/identity`):**
  - `lifecycle.py` — the **Step-0 gate (B.2)**, evaluation order **Defaulted → Dormant → New/establishing → Active**: `default_subtype()` (v1 always `unknown` → do-not-fund, conservative interim — Starr; S7 Data Steward refines, FU-301), `is_dormant()` (no active positions AND idle **> 2× median renewal gap**, own-median-else-book-median fallback; strict boundary), `is_new_establishing()` (single active position, no renewal history — Snell stays clock-running, not Disciplined), `lifecycle_state()` returns `{state, default_subtype, route, proceed_to_waterfall}`.
  - `waterfall.py` — the **5-rung waterfall (B.3)** first-match-wins + stress-override-pulls-down, plus **`rapid_reup_flag` (D-302) owned here**. `prior_paydown_at()` recomputes the prior position's paydown at the new funded_date by **reusing the S2 clock** (`elapsed_payments`/`rtr`/`amount_paid`/`est_paydown_pct`) — never reimplements the spine. PRIMARY = prior <50% paid (reuses `DISCIPLINED_RENEWAL_PAYDOWN_MIN`, no duplicate 0.50); FALLBACK = ≤45-day gap when paydown uncomputable. `is_distressed/serial/disciplined/growth/graduate` are the literal B.3 predicates; `rung_of()` composes them in spec order (stress → Distressed always pulls down; Unclassified = active but no match → `None`).
  - `confidence.py` — **borderline-driven confidence (D-306)** ∈[0.5,1.0]: `min` over the assigned rung's decisive-threshold margins (1.0 deep inside the band → 0.5 at the boundary); **absent signals omitted, never scored 0** (missing data never lowers confidence); Unclassified → 0.5 floor. `direction_of_travel()` (climbing/holding/sliding) from a prev→curr health rank (Framework 4.7).
  - `classify.py` — `classify_merchant(signals, prev=None)` composes gate → waterfall → the Framework 4.7 output object `{lifecycle_state, rung, confidence, missing_signals[], direction_of_travel}` + routing/diagnostic fields (`default_subtype`, `route`, `rapid_reup_flag`, `renewal_chain_incomplete`). `missing_signals` feeds the data-capture roadmap (never penalizes).
- **`common/eventlog/events.py` (pure, append-only, no mutation):** `classification_event` / `transition_event` (emitted only on lifecycle/rung change — the state machine) / `build_events`; ONE wide row shape `EVENT_LOG_COLUMNS` keyed `(merchant_id, event_type, event_ts)` (D-305). Timestamps passed in (deterministic; the Spark writer owns "now").
- **Schemas + maps:** `schemas/gold.py` `merchant_rung_schema()` + `event_log_schema()` (the latter built from the single `EVENT_LOG_COLUMNS` source — no drift); `field_maps.MERCHANT_RUNG_MAP` (+ `GOLD_MERCHANT_RUNG_DQ_COLUMNS` `is_gated`/`is_unclassified`, `merchant_rung_columns()`). `constants`: new enums + `RAPID_REUP_MAX_GAP_DAYS=45` + `GoldTable.MERCHANT_RUNG`/`MERCHANT_RUNG_CURRENT`/`MERCHANT_EVENT_LOG`; **reused existing `Thresholds` (0.30 ceiling, 0.50 renewal, 0.15 disciplined burden, 2.0 dormancy, 2 serial positions) — no duplicate numbers (Rule 3).**
- **Tests: 52 new tier-1 → 217 tier-1 green** (`tests/test_rung.py` 42, `tests/test_eventlog.py` 10). Cover: gate routes + order + dormancy boundary; first-match-wins + **stress override pulls a Serial-looking merchant down to Distressed**; Disciplined AND-conditions all required; D-303 unlinkable-renewal **not a disqualifier**; Growth needs rising-advance + falling-relative-burden (won't trip while burden null — honest); rapid_reup paydown-primary / day-gap-fallback / worsening-terms; confidence borderline-monotonic + missing-data-never-lowers + Unclassified-floor; direction climbing/holding/sliding; the **four B.5 merchants end-to-end** (Starr→Defaulted/unknown/do-not-fund; OBP→Dormant/win-back; Snell→New-establishing/clock-running, not Disciplined; Wolf→Active/Serial via computed rapid_reup); output-object shape; rung-map column/verdict/no-surface invariants; event append-only/no-mutation/transition-only-on-change/keying.

**Done (cloud code, 2026-06-02 — approved for `gold_test`):** `transform/gold_rung.py` — Spark driver applying the pure `classify_merchant`/`rapid_reup_flag` via UDFs over per-merchant signals assembled from `gold.merchant_clock_current` + `gold.deal_clock_current` + `gold.deals` + `gold.merchants` (reads the S2 clock; never recomputes the spine). Writes point-in-time `gold.merchant_rung` (+`_current` view, `replaceWhere` idempotent per `classify_run_date`) + append-only `gold.merchant_event_log` (classification + transition diff vs prior run). Tier-2 `tests/tier2/recon_rung.py` + `run_tier2_rung.py`.

**Tier-2 recon PASSED on `gold_test` (2026-06-02, run_date pinned 2026-06-02, `failures: []`):** built via the same `build_gold_rung` driver staged to a fresh `mri_tier2` workspace dir, serverless one-time job, staging cleaned up after. **Hard checks green** — coverage `merchant_rung`=**2,125** (= distinct merchants on `merchant_clock_current`, `(merchant_id, classify_run_date)` unique); schemas == `merchant_rung_schema()`/`event_log_schema()`; lifecycle_state ∈ enum / rung ∈ {1..5,null} / confidence ∈ [0,1] / route ∈ enum all 100%; **gated merchants 100% null-rung** + `is_gated` consistent; **Unclassified `is_unclassified` ⇔ (active AND rung null)** consistent; no-surface clean on both tables; **one classification event per merchant (2,125)** + `(merchant_id, event_type, event_ts)` unique; `_current` = latest `classify_run_date`; **state machine (2nd run 2026-06-03): prior partition unchanged (append-only), both runs' events present, `_current` advanced** (run2 transitions 0 — identical 1-day-apart signals, expected). **Diagnostics (honest, run_date 2026-06-02 sits ahead of the historical book):** lifecycle **dormant 1,458 / active 422 / new-establishing 239 / defaulted 6** (the 6 = S2 `closed_default`, Starr-class); rungs among active — **Serial 169 / Distressed 65 / Disciplined 47 / Unclassified 141**; **no Growth/Graduate** (burden null book-wide v1 → can't trip — honest, FU-301); `rapid_reup_flag` **287**; `renewal_chain_incomplete` **503** (= the S1 unlinkable renewals exactly, D-303/FU-302, never a disqualifier). **Unclassified pile quantified (exit deliverable):** 141 merchants; top missing = `disclosed_positions_cnt` 141, `est_weekly_revenue` 141, `burden_ratio` 141, `est_paydown_pct` 98, `renewal_chain_incomplete` 82. **Reference-by-name:** Wolf resolved → **active / rung 2 (Serial) / rapid_reup_flag=true** (matches B.5); Starr/OBP/Snell not name-matchable on `Account.Name` (same FU-002 nuance as S1/S2) but their lifecycle classes (defaulted/dormant/new-establishing) are present and tier-1 asserts all four end-to-end.

**PROD `gold` promotion DONE (2026-06-02, approved):** re-ran the same verified driver with `schema=gold, allow_prod=True, second_run=False` (a clean single `classify_run_date` partition — the two-run state-machine check was a `gold_test`-only verification artifact, deliberately off for prod) — **recon identical (`failures: []`)**. PROD objects: `mca_mri.gold.merchant_rung` (managed, 2,125) + `gold.merchant_rung_current` (view) + `gold.merchant_event_log` (managed, 2,125 classification events). Workspace staging cleaned up. v1 reality note: `burden_ratio`/`est_weekly_revenue` null book-wide (no feed, D-203) → burden-driven Distressed and burden-falling Growth don't trip and the Unclassified pile (141) is expected/quantified — not a defect.

**S3 exit criteria met + in PROD** (whole book classified 2,125/2,125; Unclassified pile quantified at 141; reconciles; Wolf B.5 label matches + tier-1 covers all four). Follow-ups (non-blocking): **FU-301** (NSF/statement signals when a feed/S7 lands), **FU-302** (resolve the 503 `renewal_chain_incomplete` links). Daily classify DAB job defined-as-code; scheduling/activation remains approval-gated. **S3 rung-classifier path COMPLETE.**

## Sprint 2 — progress detail

- **D-201…D-205 signed (C-016, 2026-05-31):** separate point-in-time `gold.deal_clock` / `gold.merchant_clock` (+ `*_current` views); estimated-path-only v1 (no servicing feed, D-203); plain M–F business-day counting v1 with a `holidays` hook (D-204); 55% default renewal threshold reusing `Thresholds.DEFAULT_RENEWAL_PAYDOWN` (D-205; per-funder lookup deferred to **FU-201**).
- **Shared clock logic** in `src/common/clock/{amortization,calendar,closure,rollup}.py` — pure, Spark-free-at-import (mirrors `common/identity`), reused inside the Spark UDFs/columns of `transform/gold_clock.py` (the dq.predicates ↔ dq.rules pattern). Appendix A.2 (rtr/amount_paid/balance floor/paydown cap), A.3 (business-day & weekly elapsed, term-capped), A.4 (eligible-date inverse-solve), A.5b (three-state closure; **default note dominates ~100% paydown → closed_default, never closed_clean**), A.5 (merchant roll-up: active count, weekly debit, burden, weakest balance_source, primary-position eligibility).
- **Tier-1: 165 green** (+59 clock) — hand-worked math, daily/weekly term-cap, nth-business-day inverse, the Starr defaulted-but-100% case, roll-up, all four validation merchants end-to-end vs `expected_clock`, no-surface + Merchant-Gold-contract-subset invariants.
- **Tier-2 recon PASSED on `gold_test` (2026-05-31, run_date pinned 2026-05-31, `failures: []`):** built `gold_test.deal_clock` / `merchant_clock` (+`_current`) via the same `build_gold_clock` driver staged to a fresh `mri_tier2` workspace dir, serverless job, staging cleaned up after. Hard checks green — coverage `deal_clock`=**3,959** (= gold.deals, (deal_id,run_date) unique), `merchant_clock`=**2,125** (= distinct merchants); `est_paydown_pct`∈[0,1]; `est_current_balance`≥0; closure ∈ 3 valid states; **0 default-note-but-closed_clean** (Starr invariant); `balance_source` 100% `estimated` (D-203); `est_weekly_revenue`/`burden_ratio` 100% null with `*_is_missing` 100% true; `*_current` views resolve to the single latest run_date. Diagnostics: closure closed_clean **3,059** / active **894** / **closed_default 6**; `clock_inputs_missing` 234; `rtr_checkpoint_delta` max 372,500 with **21** > tolerance (terms-contradiction flags, diagnostic only); `elapsed_at_term_cap` 3,826 and `is_eligible_now` 3,558 (both inflated because run_date 2026-05-31 sits ahead of the real funded book — correct recompute given that "today"). Reference-by-name found Wolf only (2 active positions, low paydown) — same Account-vs-Opportunity-name nuance as **FU-002**, diagnostic not a failure.
- **PROD `gold` promotion DONE (2026-05-31, approved):** re-ran the same verified driver with `schema=gold, allow_prod=true` — **recon identical (`failures: []`)**. PROD objects: `mca_mri.gold.deal_clock` (managed, 3,959), `gold.merchant_clock` (managed, 2,125), `gold.deal_clock_current` + `gold.merchant_clock_current` (views). Workspace staging cleaned up. **S2 amortization-clock path COMPLETE.**

**S2 exit criteria met.** Follow-ups (non-blocking): **FU-201** wire per-funder renewal threshold from `mca_funders`; **FU-202** review the 21 `rtr_checkpoint_delta` > tolerance deals (terms contradictions); daily recompute DAB job scheduling/activation remains approval-gated.

## Sprint 1 — progress detail

**Decisions:** D-101…D-104 approved 2026-05-31; D-105 resolved via **C-013** (PORT the AATM matching IP); **C-014** (mint own `merchant_id`, carry AATM `azure_merchant_id` via the normalized-tax_id bridge).

**Done (code, 2026-05-31):**
- **Identity IP** (`src/common/identity/`): `normalize.py` (tax_id/business_name/phone/state — ported from AATM + `normalize_state`); `match.py` (`AccountKeys`, union-find `cluster_accounts` — MasterRecordId + exact tax_id AUTO-merge; phone + name/state flagged candidates per D-102; `account_match_keys` Spark adapter); `keys.py` (`assign_merchant_ids` persisted-crosswalk stability D-101 + `match_reason_by_merchant`).
- **Gold schemas** (`src/common/schemas/gold.py`) + **field maps** (`DEAL_TABLE_MAP` = the 24 contract Deal-Table fields in order, `MERCHANT_MAP`, `MERCHANT_CROSSWALK_MAP`, gold DQ columns) + `GoldTable`/`Identity` constants + `Thresholds.WEEKS_PER_MONTH=4.33`.
- **Transforms**: `src/transform/gold_merchants.py` (crosswalk + merchant dimension + AATM `azure_merchant_id` enrichment, optional/read-only) and `src/transform/gold_deals.py` (24-field Deal Table; `term_months` Appendix A.3; renewal chain D-103; `status` from field_history; Must-capture gaps null + `*_is_missing`).
- **Tests: 106 tier-1 green** (normalize 37, match 9, keys 8, gold maps 11, + existing).

**Done (cloud, 2026-05-31) — tier-2 gold reconciliation PASSED (`gold_test`):**
- Ran `build_gold_merchants` + `build_gold_deals` into `mca_mri.gold_test.*` on **serverless** (one-time job submit; src + `recon_gold` staged as Workspace files, `run_tier2_gold` as a notebook — no bundle deploy; staging cleaned up after). Test code in `tests/tier2/recon_gold.py`.
- **Hard checks all green (`failures: []`):** `gold.deals` = **3,959 == silver.deals 3,959**; `deal_id` unique; **0 deals with null `merchant_id`**; crosswalk covers every funded Account; schemas == `deal_table_schema()`/`merchant_schema()`/`merchant_crosswalk_schema()`; **no-surface guard clean** on `gold.deals`; all 4 Must-capture gaps 100% null with `*_is_missing` 100% true.
- **Identity rollups:** **2,125 merchants** from ~2,167 funded Accounts (collapse ratio **1.0198**); merge tiers = 18 `tax_id` clusters + 2,107 singletons (no MasterRecordId merges on the funded book). **`azure_merchant_id` fill = 81.4%** (matches the C-014 ~84% tax_id-bridge estimate). **FU-002:** "Wolf Corporation" resolves to exactly 1 merchant_id (other 3 reference names live on `Opportunity.Name`, not `Account.Name` — surfaced, not failed).
- **Derivation rollups (honest, no faking):** `term_months` 3,947 populated / 12 flagged missing; renewals 1,854 → 1,351 linked + 503 `renewal_unlinkable`; `prior_factor_rate` 1,276; gap columns 0 non-null.
- **Prod `gold` promotion DONE (2026-05-31, approved):** re-ran the same verified driver with `schema=gold, allow_prod=true` — **recon identical (`failures: []`)**. Prod row counts: `mca_mri.gold.deals` **3,959**, `gold.merchants` **2,125**, `gold.merchant_crosswalk` **2,167**. Workspace staging cleaned up. **S1 identity + Deal-Table path COMPLETE.**

**S1 exit criteria met.** Follow-ups (non-blocking): calibrate the renewal-chain vs any SF link field (503 `renewal_unlinkable` to investigate in S2); FU-002 fully closeable once principal/Account-name resolution lands; `azure_merchant_id` 81.4% vs C-014's profiled 84% — small gap to reconcile (test-compute tax_id normalization parity).

## Sprint 0 — progress detail

**Gates:** G2 ✅ (Appendix A) · G3 ✅ (Appendix B) · **G1 ✅** (data audit vs live SF — bronze landed 2026-05-31; D-002 closed via C-011/C-012) · G4 ✅ (env + data rights, C-001).

**Done (offline, no cloud resources):**
- Repo scaffold: DAB (`databricks.yml`, dev/prod), `src/{ingestion,transform,common}`, `resources/`, `tests/`. `databricks bundle validate` → OK.
- Shared components (Rule 3): `constants`, `field_maps` (SPRINT_0 map as data), `dq` (predicates + Spark rules), `schemas/silver`, `io/guards` (no-surface), reserved homes for clock/identity/rung/eventlog.
- Fixtures: four validation merchants. Tests: **32 tier-1 tests passing** (constants, field-map integrity, contract consistency, DQ predicates, no-surface guard, scenario).

**Done (cloud, approved 2026-05-29):**
- `mca_mri` catalog created (managed, metastore `morgancash_uc_prd` family); schemas `bronze`/`silver`/`gold` + `_test` mirrors created and verified.
- Salesforce **External Client App** "Databricks Ingestion" created (OAuth: `api`, `refresh_token offline_access`; IP relaxed; self-authorize — FU-001 to harden pre-go-live).
- UC connection **`mri_salesforce`** created and **ACTIVE** (type SALESFORCE, OAUTH_U2M, instance `mcabrokerage.my.salesforce.com`, prod). No separate secret scope (C-009).

**Decisions confirmed 2026-05-29:** C-005 (web/auth-code OAuth), C-007 (date-sanity flags either direction), C-008 (root CLAUDE.md pointer), C-009 (no secret scope; creds in UC connection), **C-010 (all-managed tables, bronze/silver/gold)**. (C-006 superseded.)

**Done (cloud, 2026-05-31) — bronze ingestion live:**
- Lakeflow Connect ingestion pipeline **`mri_sf_ingest_bronze`** created + first run **COMPLETED** (4m 18s, 0 errors). Connection `mri_salesforce`; managed streaming tables in `mca_mri.bronze`; serverless; schedule every 6h; failure-email to venkat@. Pipeline ID `0b6d521b-…`.
- Landed: `account` 333K · `opportunity` 426K · `offer__c` 115K · `opportunityfieldhistory` 220K. Object scope = the four SPRINT_0 objects (excluded `Offer__Feed`/`Offer__History`); all columns + future-columns ON.
- **G1 field-level audit done** (profiled via Starter Warehouse, read-only). Funded book = **3,959** opps (`StageName='Funded'`). **C-011** (object names) + **C-012** (field source-of-truth) logged; **D-002 closed**.
  - Economics from **selected offer** (`Offer__c` where `Select_Offer__c=true`): `factor_rate`/`payback_amount`/`payment_amount` use the **underscored** Offer fields; `funded_amount` from `Opportunity.Funded_Amount__c`; merchant link = `Opportunity.AccountId`; FICO from `FICO__c` (string).
  - DQ found: ~28 funded deals have >1 selected offer (dedup on latest `LastModifiedDate`); 2 have none (fallback + flag). `field_maps.DEALS_MAP` updated to real API names; **32 tier-1 tests green**.

**Done (code, 2026-05-31) — post-G1 build:**
- **Pipeline-as-code**: `resources/ingestion_pipeline.yml` authored (pipeline + 6h job), mirrors live resources; `databricks bundle validate -t dev` → OK. Bind-before-deploy note included (avoids duplicate pipeline).
- **bronze→silver `deals` transform** (`src/transform/silver_deals.py`): implements C-012 — `resolve_selected_offer` (filter `Select_Offer__c=true`, dedup latest `LastModifiedDate`, `multi_selected_offer` flag); `select_rename_deals` (funded book, left-join selected offer, economics-from-offer with Opportunity fallback, `selected_offer_missing` flag, FICO string→int); `build_silver_deals` writes managed `mca_mri.silver.deals`.
- **C-007 now implemented**: `date_sanity_flag` rewritten to flag a large funded/created gap in **either** direction (threshold `DATE_SANITY_GAP_DAYS=365`, calibratable) across pure-Python + Spark mirrors; the old literal `funded>created` rule both over-flagged latency and missed the migration artifact. Added `BronzeTable` constants. **34 tier-1 tests green.**

**Done (cloud, 2026-05-31) — tier-2 reconciliation PASSED (`silver_test`):**
- Ran `build_silver_deals` into `mca_mri.silver_test.deals` on **serverless** (one-time job submit; src + `recon_silver_deals` staged as Workspace **files**, driver `run_tier2` as a notebook — no bundle deploy, so the live pipeline is untouched/not duplicated). Test code lives in `tests/tier2/`.
- **Hard checks all green:** silver count **3,959 == bronze funded 3,959** (exact); schema == `deals_schema()` (column names + order); **no-surface guard clean** on the consumed projection (the 3 `_sf_stored_*` checkpoint cols stay on the base table only); selected-offer anomalies within G1 band — `selected_offer_missing=2` (=G1), `multi_selected_offer=26` (G1~28).
- **Diagnostic DQ rollups (informational, never drop rows):** `date_sanity_flag=1686` (~43% — large funded/created gaps, consistent with the SF migration stamping `CreatedDate`; C-007 working as designed, **threshold calibration to revisit**); `months_in_business_is_missing=3750` (~95%, matches G1 sparsity); `fico_is_missing=382`; `rtr_check_flag=21`.
- **Reference-merchant-by-name check = 0/4 (expected, not a failure):** `Opportunity.Name` is the auto-generated opportunity label, not the merchant business name (that lives on `Account.Name`). Validating the four CLAUDE.md §8 merchants needs the Account join → **deferred to S1 identity resolution**. Logged as **FU-002**.

**Done (cloud, 2026-05-31) — prod `silver.deals` promotion:**
- Ran the same verified driver against **`schema=silver, allow_prod=true`** (serverless one-time job). **`mca_mri.silver.deals` built — recon identical to `silver_test`:** 3,959 == bronze funded, schema == `deals_schema()`, no-surface clean, `selected_offer_missing=2` / `multi_selected_offer=26`.

**Done (code + cloud, 2026-05-31) — optional silver tables `offers` + `field_history`:**
- **`src/transform/silver_offers.py`** (raw Offer__c catalogue, all offers — underscored economics per C-012, `is_deleted` carried not filtered) + **`src/transform/silver_field_history.py`** (OpportunityFieldHistory event source for S3 renewal-cadence reconstruction). Added `OFFERS_MAP`, extended `FIELD_HISTORY_MAP` (+`history_id`, `data_type`), `offers_schema()`, tier-1 tests. **41 tier-1 tests green.**
- **Built + reconciled on `silver_test`, then PROMOTED TO PROD** (serverless; `tests/tier2/recon_aux.py` + `run_tier2_aux`). Both exact to bronze, schema-matched, **0 FK orphans**: `silver.offers` = **57,586** (5,143 selected), `silver.field_history` = **220,172** (13,705 StageName events). Workspace staging cleaned up.

**Next (all deferred / non-blocking):**
- FU-001 (pre-go-live): lock External Client App pre-auth to the integration user; switch pipeline run-as to a service principal.
- FU-002 (S1): validate the four reference merchants via Account join once identity resolution lands.
- Calibrate `DATE_SANITY_GAP_DAYS` (currently 365 → ~43% flagged from the SF migration; diagnostic only).
- Bind `resources/ingestion_pipeline.yml` to the live pipeline before any `bundle deploy` (avoids duplicate pipeline).

**Exit criteria:** [SPRINT_0_PLAN](sprints/SPRINT_0_PLAN.md) §9 — bronze landed + **all three silver tables (`deals`, `offers`, `field_history`) built, reconciled to bronze, and in prod `silver`** ✅. **S0 ingestion→silver path COMPLETE.**
