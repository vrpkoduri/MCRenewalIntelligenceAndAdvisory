# Decisions Log

Append-only record of decisions you've signed off on, and open decisions awaiting your call (GENERAL_INSTRUCTIONS Rule 5). Newest first.

## Confirmed

| ID | Date | Decision | Rationale |
|---|---|---|---|
| C-032 | 2026-07-20 | **D-808 — near-payoff structure refinement + advisory buyout-degrade, signed off** ("fix S5 structure logic too") after the S8 full-book `gold_test` run surfaced two issues on the buyout path. **(1) S5 `recommend_structure` near-payoff ceiling:** at `paydown ≥ NEAR_PAYOFF_PAYDOWN` (**0.90**, new `Thresholds` constant, calibratable) a position is essentially complete, so the honest structure is **RENEWAL (finish/renew), never BUYOUT** — the multi-position buyout rule no longer fires for a merchant 99.9% paid with $12 left (the run produced ~a dozen nonsensical "consolidate a near-zero balance" recommendations, e.g. double-dip cost $2.70). This is a SHARED-spine change (benefits S5 offers too), but S5 offers are gated on FU-501 so the only live consumer today is the S8 advisory (`gold_test`). **(2) Advisory Composer v3:** every REJECTED row in the full-book run was a buyout advisory whose LLM quoted a **new/reduced/consolidated payment the spine never computed** (a new-deal term = FU-501's job); the grounding validator correctly rejected all of them (0 reached a merchant). v3 explicitly forbids stating any new/consolidated/single post-consolidation payment — the advisor speaks only the CURRENT figures (weekly/daily payment, balance, positions, double-dip cost). **So honest buyout-consolidation advice with real consolidated terms remains gated on FU-501; until then those merchants get a grounded current-burden / finish-paying framing.** +2 tier-1 (structure near-payoff) → **355 green.** | The full-book run did its job — it found a real advice-quality gap (nonsensical near-payoff buyouts) AND confirmed the grounding gate blocks uncomputed consolidation figures. Fixing `recommend_structure` at the shared source (not papering over it in the advisory) is the correct place; v3 stops the model generating figures it will only have once FU-501 lands. Neither was a safety failure (the gate held), so this is a quality pass, not a defect fix. |
| C-031 | 2026-07-18 | **Sprint 8 decisions D-801…D-807 signed off** ("go ahead with the recommendations") — recommendations adopted as written (detail in [SPRINT_8_PLAN](sprints/SPRINT_8_PLAN.md) §8). The **Advisory Layer**: two agents that **articulate** the spine's computed facts into honest merchant-facing guidance, behind a **first-class deterministic compliance gate** (Framework §2.3 honesty / §2.4 gate-is-real / §5.9 agents-never-compute). **D-801** the compliance gate = a pure deterministic `common/compliance/` block (classify + disclosure + gate), a **HARD BLOCK** (ungrounded / suppressed-offer-pitched / missing-disclosure → BLOCKED, never deliverable), **merchant-facing scope only** (S4 rep guidance untouched) — realizes the S5 `compliance_gate_hook` (D-508); **D-802** the Advisory Composer speaks **only** a deterministic fact pack (value + source gold field + run_date each), a grounding validator rejects any numeric token not in the pack, output is a structured advisory record (reuses `common/agents/grounding.py`); **D-803** the Structure Advisor **articulates only** the S5 `structure_evaluation`/`suitability_verdict` (never recomputes the double-dip math, cannot un-suppress a suppressed offer); **D-804** separate point-in-time `gold.merchant_advisory` (+`_current`) keyed `(merchant_id, advisory_run_date)`, carrying grounded_refs/confidence/model_version/`compliance_status`/required_disclosures/review_status — **stored, not delivered**; **D-805** state-aware disclosure = a config-driven `disclosure_rules` table keyed by `governing_state` (seeded CA / NY-CFDL / UT / VA + others as researched), v1 **flags the regime + requires a disclosure block, does NOT draft legal language** (counsel owns wording; counsel to review the state list before any cloud run); **D-806** deterministic advice-vs-specific-offer classification (specific-offer = names concrete terms → strict path: disclosure required + suitability must be SURFACE; the agent labels intent, the gate enforces); **D-807** a permanent tier-2 quality gate on a labeled advisory sample (four merchants + N more) scoring grounding (**0 invented numbers — hard 100%**) + gate correctness + honesty (recommends *wait / don't take money* where the math says so). **This first pass builds OFFLINE deterministic + pure-agent modules only** (the S7 offline-first pattern); the FM/cloud `transform/gold_advisory.py`, any real FM calls, the `gold_test`/PROD runs, and **any delivery** remain individually gated (Rule 5). **S8 composes + gates; it does NOT send** (no outbound comms, no SF write, no app). | Lands the advisory layer honestly: the agents write language but deterministic code owns every fact and every compliance verdict, so an agent can neither invent a number nor self-certify compliance (§2.3/§2.4/§5.9). Reuses the proven S7 grounding/gate/labeled-accuracy machinery. Every irreversible step (cloud, delivery) stays behind explicit approval; D-805 keeps legal wording with counsel. |
| C-030 | 2026-07-14 | **S7 Phase-2 Statement Analyst PROMOTED TO PROD `gold` (explicit approval; "secret rotation later").** Two gated steps on already-fetched data (no SF re-ingest, no spine recompute — advisory-only): (1) OCR → PROD `silver.statement_text` (`allow_prod=True`) — 324 files, 317 text-OK / 7 needs_ocr / 1 parse_error, 79 deals; (2) `build_gold_statement_extraction(schema="gold", allow_prod=True)` — 79 covered deals → **237 rows (14 applied / 223 review / 0 rejected)** in `gold.merchant_extraction` (+`gold.statement_extraction_audit` 79 rows + 237 `agent_extraction` events), model_version v2. **Post-promotion verification (C-029 fix held in PROD):** `merchant_extraction_current` carries BOTH streams — `default_subtype` 6 (**applied 5** — Starr et al. intact, NOT evicted) + statement types 79 each; `current_run_dates` [2026-06-05, 2026-07-14]. Volume `bronze.statements_raw` grants `{}` (owner-scoped, D-712 OK). **Advisory-only (C-026 #1): the rung waterfall is unchanged; no S3/S4 re-run triggered.** | Completes S7 Phase-2 in PROD: the funded book's bank statements now yield grounded, gated, audited true-concurrent-positions + burden/revenue for the covered cohort, surfaced without altering any classification. Remaining are non-blocking follow-ups (dashboard surfacing, FU-703 rotate, FU-704 revenue guard, FU-705 funder registry, FU-706 routing-team alignment, FU-707 daily scheduling) + the PR to `main`. |
| C-029 | 2026-07-13 | **Pre-promotion review caught + fixed a `_current` multi-stream bug (BLOCKER) before PROD.** `gold.merchant_extraction._current` was defined as global-`MAX(extraction_run_date)`. But the table is multi-stream — the Data Steward's `default_subtype` rows (PROD run_date 2026-06-05) + the Statement Analyst's positions/burden/revenue (later run_date). A global-MAX view would show **only the newest run_date**, evicting `default_subtype` from `_current` — and `gold_rung._resolved_default_subtypes` reads exactly that view (`DEFAULT_SUBTYPE`+`APPLIED`), so the **next S3 recompute would silently revert every resolved default** (Starr `true_default→distressed-exit` back to `unknown→do-not-fund`). **Fix:** `_create_current_view` (shared by both extraction drivers, Rule 3) → **latest row per `(merchant_id, deal_id, extraction_type)`**, so each stream stays current independently; `recon_extraction` `_current` assertions updated to be multi-stream-aware (this-run default_subtype rows must be current). **Verified on `gold_test`:** with 4 streams across run_dates 2026-06-05…07-13, the fixed `_current` = `default_subtype` 6 + each statement type 79 (latest), `current_default_subtype_applied` **5** (was 0 pre-fix). Also correctly collapses multiple statement runs to the latest per deal. 324 tier-1 green. | A global-latest view silently breaks a multi-writer point-in-time table — exactly the kind of integrity regression the ask-before-deploy review (Rule 5) exists to catch. Fix is localized to the one shared helper; other single-stream `_current` views (clock/rung/activation/predictions) are unaffected. PROD promotion now safe. |
| C-028 | 2026-07-13 | **D-711 positions accuracy gate PASSED (10/10) on `gold_test` — Statement Analyst v2 validated; PROD promotion now the only remaining gated step.** Operator reviewed the v2 extractions on the 10-deal sample ("all look good and as expected") → encoded as `recon_statement_analyst.STATEMENT_LABELS` (true position counts: A1a 0, 5 Star 2, Tom Snell 2, Hjm 2, All Point 4, Wolf 3, Wolfe Pack 1, David Meyers 0, Findley 0, Flener 2). Re-ran the tier-2 recon (v2, `databricks-claude-sonnet-4-5`, temperature 0) over all 79 covered deals → 237 rows, mechanical gates green (schema/coverage/keys/grounding/no-surface/`_current`), **labeled_accuracy 1.0 (10/10), `failures: []`**. Positions-only gate (±1), scored on the extracted count regardless of review_status so accuracy is decoupled from the freshness surfacing gate (#2); **the ±1 tolerance correctly absorbed A1a's known 0-vs-1 boundary wobble** (Summit Hosting counted or not run-to-run). Revenue accuracy intentionally excluded (advisory/soft → FU-704). The gate is now a **permanent PROD-promotion gate** (mirrors D-706). | Closes the D-711 accuracy question for the pilot with operator-confirmed ground truth: v2's tightened position definition (leases/loans/vendor/card excluded) reads the funded book's statements accurately within ±1. The build is validated end-to-end (fetch→OCR→agent→gate) on `gold_test`; PROD is a separate explicit approval (Rule 5). FU-705 (funder registry) stays a separate later process; FU-704 (revenue guard) deferred. |
| C-027 | 2026-07-09 | **Reuse check — the routing side's statement extraction does NOT cover MRI's funded book (0 overlap) → the Statement Analyst build STANDS.** Prompted by "we have the funder registry in the lakehouse," investigated `mca_funders`: found not just `silver.funders` (a registry) but **`gold.merchant_submission_positions`** (lender_name/frequency/payback/principal per submission) + **`merchant_submission_monthly_metrics`** (revenue_deposits, days_with_nsf, avg_daily_balance) + **`merchant_submissions`** (existing_positions_count, combined_mca_balance, monthly_revenue, fein) — i.e. the routing team already does statement-based position/metric extraction. **BUT coverage is disjoint from the funded book:** positions are `bank_statement_inferred` over only **191 submissions**; `source_application_id` is `OM_…` (AATM-style) ≠ MRI's `a0o…` SF Application_Submission (**0 app-id join**); and the **tax_id/fein join = 0** (MRI 2,053 funded-with-taxid × 218 submissions-with-fein → **0 overlap**). This is the same new-deal-funnel-vs-funded-book disjointness S5 found (C-019). **So there is no reuse shortcut for the funded book — MRI's Statement Analyst (SF Files → Application_Submission → funded deal) is the only source for the funded book's statements, and is validated as necessary, not redundant.** **`silver.funders` = 12 *partner* funders** (Credibly, IOU, LG, NewCo, OnDeck/Headway, Forward Financing, …), NOT the competitor MCAs on statements → a **partial** seed for the FU-705 registry, to be combined with the routing side's broader `lender_name` vocabulary (BlueVine, Fundbox, Itria, Prosperity Gold, SoFi, …) + the funders MRI observes. **New follow-up FU-706:** align with the routing team on a **shared funder registry + a common statement-extraction service** so two teams don't extract statements divergently (their `monthly_metrics.days_with_nsf` is also the NSF signal FU-301 wants). | The right instinct to check before building more — but the data is unambiguous (0 overlap by both app-id and tax_id), so reuse isn't viable for the funded book and the Phase-2 build proceeds. The discovery still pays off: it validates our approach matches house practice, seeds the funder registry, and surfaces a real cross-team-alignment opportunity (FU-706) instead of silent duplication. |
| C-026 | 2026-06-09 | **S7 Phase-2 re-scoped build APPROVED + the three burden-policy calls resolved.** After the coverage spike (C-025 §1.6 — 79 funded deals / 2.0% of book, 28.4% of 2025+ fundings, ~35 active merchants; fetch scope 784 files / 0.42 GB), the build is approved as a **go-forward capability + ~35-active-merchant burden enrichment, NOT a retroactive book-wide fix.** Three policy decisions: **(#1 — advisory-only, "leave classification as-is")** statement-derived `burden_ratio` + true concurrent positions are recorded in `gold.merchant_extraction` and **surfaced** (dashboard / a Book-Health covered cohort, labeled `burden_source=statement`), but the **S3 rung waterfall keeps IGNORING burden** (as it does book-wide today) — a covered merchant is NOT downgraded to Distressed/Serial purely because we hold their statement. **This OVERRIDES D-710's "the rung re-runs sharper" intent:** the agent extracts + surfaces; the spine's classification is unchanged (no covered-vs-uncovered asymmetry; D-306 "missing data never penalizes" preserved; §5.9 honored even more strictly — the agent never influences the spine). **(#2 — freshness)** statement-derived signals apply only within a freshness window (`STATEMENT_FRESHNESS_MAX_DAYS`); each extraction carries the statement's `as_of_date`; stale → not surfaced as current (gate to REVIEW), never persisted as live truth. **(#3 — revenue softness)** the agent classifies only **operating-revenue** deposits (excludes transfers / loan proceeds / owner injections); `est_weekly_revenue` carries **lower confidence** than positions/debit (deterministic haircut) and a **looser tolerance** in the D-711 accuracy gate (positions stay ±1; revenue a % band). | The coverage truth makes Phase 2 a forward-looking enrichment, not a book-wide burden recompute — so the honest move is to surface real burden where we have it WITHOUT judging merchants unevenly (#1), guard against stale funding-moment snapshots leaking into a live calc (#2), and treat the deposits→revenue inference (the softest, burden-denominator signal) conservatively (#3). FU-702 (upstream ops: populate `Application_Submission__c` + tag `Document_Type__c` at intake) is the real coverage multiplier and runs as a parallel track. **Binary-fetch path PROVEN 2026-06-09:** headless Client Credentials Flow on the existing External Client App (Run-As `integrations@morgancash.com`, API Enabled + View All Data read-only) + secret scope `mri-salesforce-api`; read-only test downloaded a `ContentVersion`/`VersionData` PDF (200 / 3.32 MB / size-matched / no 403). **FU-703 (security):** Consumer Key/Secret were briefly stored as plaintext secret-scope key *names* during setup (internal-only exposure; cleaned up by delete+recreate) → rotate the Consumer Secret + re-auth ingestion (same app), or use a dedicated app. |
| C-025 | 2026-06-06 | **S7 Phase-2 (Statement Analyst) decisions D-707…D-713 signed off** ("ok go") — recommendations adopted as written (detail in [D-702 spike + Phase-2 plan](D-702_statement_ingestion_spike.md)). Path: **D-707** Lakeflow ingests file **metadata**, a separate gated read-only fetch lands **binaries** in a UC Volume + `bronze.statement_files`; **D-708** deterministic **silver OCR** (`ai_parse_document`/pdfplumber) → `silver.statement_text`, agent classifies from text; **D-709** funding-moment statements only (~3 most-recent months at funding); **D-710** clock reads APPLIED extraction as **optional enrichment** (other-funder positions ADD to `active_position_cnt`, deposits fill `est_weekly_revenue` → real `burden_ratio`; absent/low-conf ⇒ today's behavior) — advances **FU-301**; **D-711** labeled sample mirrors D-706 (Wolf central; positions ±1; weekly_debit/revenue ±10%; ACCURACY_BAR 0.80; permanent tier-2 gate + regression guards); **D-712** statements/OCR text stay in governed UC, **no-surface guard** for raw account numbers / running balances (only derived aggregates leave); **D-713** sample-first, OCR cached in silver, spend-gated. **No extraction-table schema change** (the `concurrent_positions`/`weekly_debit`/`est_weekly_revenue` types + generic `value` already exist). **Each cloud write remains individually gated** — `gold_test` first, PROD needs explicit approval + `allow_prod=True` (Rule 5). **Read-only metadata probe RUN 2026-06-06 (C-025 option A — metadata-only `ContentDocument` ingest to `bronze_test`, NO blobs, fully torn down):** Salesforce Files is the store — **24,285 files / 13.4 GB; 23,700 PDF (97.6%) + ≈467 images; ≥7,552 clearly bank statements, mostly 2024-dated** → Phase 2 well justified. Build must handle title-keywords-undercount (classifier needs filetype+date+linked-deal context), OCR for scanned/image files, and ZIP/multi-month bundles. **Coverage RESOLVED 2026-06-07/09 (spike §1.6):** the connector **auto-drops base64** (verified — `VersionData` never lands), so `ContentVersion` *metadata* was safely probed (then torn down). Linkage = `FirstPublishLocationId` → `a0o` submission → `Opportunity.Application_Submission__c` (no ContentDocumentLink needed); **`Document_Type__c`** gives a structured "Bank Statement" tag (998) + title heuristic → 8,146 statement files org-wide. **Coverage: 79 funded deals (2.0% of book; 28.4% of 2025+ fundings — capture started ~2025); 35 active merchants (4.3% of active book). Wolf has 5 statement files (labeled-sample anchor); Starr none.** Fetch scope collapses to **784 funded-linked files / 0.42 GB (324 statements / 0.17 GB)** — trivial OCR/LLM cost. **Re-scope verdict: Phase 2 = a go-forward capability + ~35-active-merchant enrichment, NOT a retroactive book-wide burden fix** — honest population quantified; grows with every new funding. **New follow-up FU-702:** upstream ops fixes (populate `Application_Submission__c` on every funded Opportunity; tag `Document_Type__c` at intake) multiply coverage for free. | Adopts the §5.9-faithful path (agent extracts, deterministic tools count, spine computes) reusing the proven Phase-1 grounding/gate/accuracy-gate machinery; defers every irreversible cloud step behind the probe + per-write approval so we learn coverage before spending on OCR/LLM or pulling sensitive blobs. |
| C-024 | 2026-06-05 | **D-706 labeled-sample accuracy PASS (6/6) — Data Steward v2; PROD `gold` UNBLOCKED (pending explicit promote approval, Rule 5).** The v1 run (C-023) surfaced two adjudication questions, both resolved by the operator on the 6-deal labeled sample: **(1) a clawback on a *defaulted* deal = `true_default`** (the funder reclaiming the broker's commission — a loss, NOT an early payoff); **(2) an explicit "defaulted" note = `true_default`** even with no further sub-cause detail (a default IS a loss; early_payoff/restructured are the exceptions that require positive evidence) — the agent only abstains to `unknown` when there is *no* default-cause signal at all. **Fix:** Data Steward prompt **v2** (`model_version data-steward/claude-sonnet-4-5/v2`) makes an explicit "defaulted" override a clawback mention; the labeled sample + an accuracy gate (`recon_extraction.DEFAULT_SUBTYPE_LABELS`, `ACCURACY_BAR=0.80`, + a true_default-misrouted-as-early_payoff regression guard) were added to the tier-2 recon. **Re-run on `gold_test`: labeled_accuracy 1.0 (6/6), `failures: []`** — 5 true_default→distressed-exit (Ab Powers, Con Alma, Luxury Dentistry, Sai, Starr), 1 unknown→do-not-fund+review (Zeek, correctly abstained; "mtd, ar, 110K transfer" has no default-cause signal). **The accuracy gate is now a permanent PROD-promotion gate. PROMOTED TO PROD `gold` 2026-06-05** (`schema=gold, allow_prod=True`, explicit approval): `mca_mri.gold.merchant_extraction` (+`_current`) + 6 `agent_extraction` events written, the S3 classifier re-run on PROD routed the 5 resolved defaults → distressed-exit (Starr `true_default`→distressed-exit), `failures: []`, labeled accuracy 1.0. | D-706 did exactly its job — caught a real v1 error (clawback→early-payoff) AND forced an explicit labeling policy for bare "defaulted" notes, then verified the fix end-to-end. The agentic layer is now grounded, gated, logged, AND accuracy-bounded on operator-confirmed truth — the bar for trusting agent output on the spine re-run. PROD write stays a separate explicit approval (Rule 5). |
| C-023 | 2026-06-05 | **S7 Phase-1 Data Steward — built + RUN on `gold_test` (tier-2 PASS, `failures: []`).** The LLM agent (Databricks Foundation Model **`databricks-claude-sonnet-4-5`** via the `databricks-sdk` serving client, temperature 0, model_version `data-steward/claude-sonnet-4-5/v1`) sub-typed all **6 `closed_default`** deals' `silver.deals.notes`; the deterministic tools gated + grounded; the S3 classifier was re-run reading APPLIED results. **Outcome: 6/6 extracted (coverage exact, keys unique, all grounded, no-surface clean), 3 APPLIED / 3 REVIEW / 0 REJECTED → unknown-pile resolution 3/6.** Routes: **Luxury Dentistry NYC** "Default on forward" → true_default → **distressed-exit** (0.95); **Sai Adjustments** "$400 claw back" + **Starr Window Tinting Llc** "$250 Claw back" → early_payoff → **win-back** (0.95); **Ab Powers / Con Alma** Notes say only "defaulted" (no cause) → conf 0.30 → stay `unknown` → **do-not-fund** + review; **Zeek Interactive** no usable cause → `unknown` → review. The agent never wrote a spine table (only `gold.merchant_extraction` + 6 `agent_extraction` events). PROD `gold` promotion **gated** pending the D-706 labeled-sample accuracy pass. **OPEN finding (honest):** the synonym **clawback → early-payoff → win-back** is confidently applied, but a "$N clawback" on a *defaulted* deal is genuinely ambiguous (early-payoff clawback = healthy vs default-triggered commission clawback = loss) — this is precisely what **D-706** must adjudicate before prod; until then Starr/Sai move to win-back only in `gold_test`. **Also fixed FU-701** (latent: `gold_rung.compute_event_log` threw `UNRESOLVED_COLUMN current_state` once S4 widened the event log and S3 was re-run — now null-fills missing wide-log columns before the schema projection). | Validates the §5.9 mechanism end-to-end on real data — grounded, gated, logged, auditable, and *honest about uncertainty* (it refuses to guess the 3 cause-less "defaulted" notes). Surfaces real advisory value (3 mis-bucketed defaults reclassified, incl. the Starr fixture, with cited evidence) while keeping the semantic-accuracy question explicit and prod-gated. |
| C-022 | 2026-06-05 | **Sprint 7 decisions D-701…D-706 signed off** (agentic extraction; offline deterministic foundation proceeding, LLM calls + cloud gated). **D-703 → Data Steward FIRST (Phase 1, build now)** on existing `silver.deals.notes` (resolve `default_subtype`: true-default/early-payoff/restructured + anomaly flags; validate vs Starr); **Statement Analyst = Phase 2, deferred** (statements confirmed to live in SF Documents/attachments — D-702 ingestion spike when Phase 2 starts). **D-701 → Databricks-native LLM**: Foundation Model APIs (Claude/Llama) + Mosaic AI Agent Framework (governance/lineage/audit); batch; LLM spend approved + gated. **D-704 → point-in-time `gold.merchant_extraction`** (+`_current`) with source_ref/confidence/model_version/review_status; the spine consumes it as optional enrichment via the normal re-run — **the agent never writes spine tables**. **D-705 → grounding + human-review**: every extraction carries source_ref + citation + confidence; `< AGENT_CONFIDENCE_REVIEW_MIN` → review queue, never auto-applied; agents call deterministic tools for all counting/classification; full event-log audit incl. model_version. **D-706 → accuracy bar on a hand-labeled sample** (4 merchants + N more), "sane + improving" acceptance calibrated on the first sample. | Framework §5.9: agents **extract**, the spine **computes** — auditability preserved. Phase 1 ships immediate value (resolve the interim `unknown` defaults incl. Starr) with trivial LLM cost on existing data; Phase 2 (statements → true positions/burden, FU-301) follows once the statement corpus is wired. Databricks-native keeps the whole agentic layer governed + logged. |
| C-021 | 2026-06-02 | **v1 activation surface = a read-only Databricks AI/BI (Lakeview) dashboard over PROD `gold`** (instead of the Salesforce write-back for now — **FU-401 deferred**). Three sections (signed): **Book Health scoreboard** (management trends/distributions), **Daily Queue** (floor — `daily_queue` ordered by `queue_rank` with state/play/SLA/next-action), **Merchant 360** (per-merchant drill across rung/clock/activation/predictions). Authored **as-code** (`resources/`), deployed via the Lakeview Dashboards API on the Starter Warehouse; read-only over the gold `_current` views (no writes, no SF coupling). **S6 predictions promoted to PROD first** (lifetimes v1, the gold_test-verified driver with `schema=gold, allow_prod=True`) so the dashboard shows the full picture — the p_alive calibration caveat persists and is refined by FU-602. | A dashboard is the Framework's own "renderer over the gold table" (§5.4/§5.8) — it delivers management + floor visibility now with ZERO outward-facing risk (read-only), while reps-in-Salesforce stays the later, SF-setup-dependent FU-401. Promoting S6 v1 to prod makes the prediction tiles real; honesty preserved via `insufficient_history` + confidence + the documented calibration follow-up. |
| C-020 | 2026-06-02 | **Sprint 6 decisions D-601…D-609 signed off** (offline pure modules proceeding; model fitting + serving gated, Rule 5). First ML sprint — **ADOPT PyMC-Marketing (BG/NBD + Gamma-Gamma + CLV) + lifelines (Cox PH + KM) + MLflow**, not hand-built (CLAUDE.md §4). **D-601** RFM: 1 row/merchant, event = funded advance, frequency = deal_count−1, recency = last−first, T = today−first, monetary = avg funded_amount. **D-602** adopt the libs on a Databricks ML runtime; fitting is Databricks-only (not tier-1 local). **D-603** sparse-data: hierarchical/book-level priors + Cox **censoring** for not-yet-renewed + explicit **`insufficient_history`** flag (repeat events < `INSUFFICIENT_HISTORY_MIN_EVENTS`=1) + `prediction_confidence` = normalized posterior width. **D-604** separate point-in-time `gold.merchant_predictions` (+`_current`). **D-605** MLflow tracking+registry + **batch inference** to gold + `prediction` events; defer the real-time Model Serving endpoint. **D-606** CLV `future_t`=12 months, `discount_rate`≈12% annual (calibratable). **D-607** Cox: event = took capital vs censored; duration = interval-to-next (else tenure censor); covariates = factor_trend, active_position_cnt, payment_health, industry_vertical — **drop `burden_ratio`** (null book-wide v1, FU-301). **D-608** time-split backtest; calibration + ordering + KM baseline per rung; accept on "sane + explainable". **D-609** emit `prediction` events; the S4 queue refines ordering by p_defection + predicted_next_event_date (never overrides the deterministic gates; distress stays signal-driven). | The readiness spike (2026-06-02) validated the approach: **796/2,125 (37.5%) merchants are repeat** (the training population; 358 active) and **1,329 (62.5%) single-deal** → exactly the sparse-data case the §11.2 toolkit was chosen for, so hierarchical priors + censoring + an honest `insufficient_history` bucket are necessary, not optional. The demand clock has real signal (1,834 intervals, median 81 days). Models stay auditable (MLflow-versioned, logged) — part of the spine, not the agentic layer. **New follow-up FU-601:** the real `Type` set is New Business / Renewal / Stack / Add-On (no "Buyout"; Stack/Add-On are repeat advances) — fix `DealType` + `has_renewal` semantics in S3/S4/S5 (limited impact; S6 unaffected — its repeat signal is `deal_count ≥ 2`). |
| C-019 | 2026-06-02 | **Sprint 5 decisions D-501…D-508 signed off** (offline pure modules built; cloud build + cross-team handoff gated, Rule 5). **D-501 → REUSE via batch handoff (NOT invoke, NOT reuse-existing-evaluations):** the spike showed the existing routing OUTPUTS cover only the new-deal population (121 merchants) with **0 id-overlap** to the funded book, so MRI **builds + lands `v_funder_input`-shaped profiles** for its 2,125 funded merchants, the **existing routing engine evaluates them on a cadence** (owned by the routing side — no engine rebuild, no porting), and **MRI reads the results back** and maps them to `gold.merchant_offers`. **D-502 → no usable id join today** (MRI `azure_merchant_id` vs funder ids = 0 overlap; `v_funder_input` carries no tax_id), so the handoff MUST carry **MRI `merchant_id` through the engine run** (results keyed back to MRI) — defined in the handoff contract (**FU-501**). **D-503** profile = MRI gold fields present (state/FICO/TIB/positions/industry); revenue/NSF/bankruptcy absent → engine missing-data verdicts, never faked. **D-504** `eligible_offer_types` = candidate (clock/rung/state) ∩ funder match. **D-505** `max_sustainable_advance` null+flag in v1 (revenue-dependent, FU-301). **D-506** renewal-vs-buyout suitability gate BUILT (double-dip cost + wait/buyout/renewal; suppress unsuitable). **D-507** point-in-time `gold.merchant_offers` (+`_current`). **D-508** compliance = interface-only (S8). | Honors "reuse, don't rebuild" (CLAUDE.md §6) without coupling MRI into another team's engine code: a clean profile→evaluate→read-back data handoff, the routing side keeps owning the matching IP + box currency, and MRI ships the offer integration + the honesty (suitability) gate now. **New follow-up FU-501:** define the profile-handoff contract with the routing team (landing table + MRI-merchant_id round-trip + cadence). |
| C-018 | 2026-06-02 | **Sprint 4 decisions D-401…D-406 signed off** (offline pure modules proceeding; cloud build + serving gated, Rule 5). **D-401 → state machine** `current_state` ∈ {clock-running, approaching, in-market, renewed, lost-winback}: `in-market`=active & `is_eligible_now`; `approaching`=active & `est_renewal_eligible_date` within `APPROACHING_WINDOW_DAYS`=**30** of today; `renewed`=active & last advance funded within `RENEWED_WINDOW_DAYS`=**30**; `clock-running`=active otherwise; `lost-winback`=dormant/defaulted (gated lifecycle). **D-402 → named plays** = deterministic priority matrix over (lifecycle/route, rung, current_state, direction): do-not-fund-review › win-back › new-establishing-nurture › distressed-stabilize › slide-intervention › serial-renewal-vs-buyout › in-market-renewal › approaching-prep › growth-upsell › graduate-referral › review-unclassified › disciplined-reinforce. **SLA tiers (business days): 2** (distressed/slide), **5** (in-market/serial/growth/do-not-fund-review), **10** (rest); `play_sla_due` = nth-business-day-after (reuses `common.clock.calendar`). `play_owner` ← SF Opportunity OwnerId (ingest) else null + `play_owner_is_missing` (FU-101). next_tactical_action / next_strategic_nudge = grounded deterministic per-play templates (internal rep guidance — NOT merchant comms). **D-403 → reverse-ETL STOPS at the Lakebase/gold serving layer for S4 (NO Salesforce write this sprint).** Floor reads rung/state/play from gold views (`daily_queue` + `merchant_activation_current`); a Lakebase sync + the actual governed SF write-back (dedicated MRI fields, sandbox-first, `allow_sf_write`-gated) are **deferred to a later gated step → FU-401**. **D-404 → Book Health** as a point-in-time table family keyed `report_date` (+ `_current` views per Framework-5.8 view); **v1 metrics** = rung distribution + drift (event log), default/restructure trend, sliding count, approaching pipeline, concentration risk, partial renewal-capture; **deferred** (inputs are S5/S6/S8) = aggregate-LTV, defection rate/destination, offer-acceptance, value-to-ask, comms time-to-contact/SLA-adherence. **D-405 → S4 absorbs the state machine + plays + SLA + next-actions** (the Build Plan listed them under S3; the signed S3 plan C-017 deferred activation to S4). **D-406 → `gold.daily_queue` view** over `merchant_activation_current` + clock, ordered sliding-first → in-market/approaching → `play_sla_due` → confidence. | Lands the intelligence on the floor with zero outward-facing risk in v1 (read-only serving layer, no SF write), defers the irreversible Salesforce write-back to its own approval-gated step (FU-401), and builds Book Health only on inputs that exist today — honest, reconcilable, and consistent with the S2/S3 point-in-time pattern. |
| C-017 | 2026-06-01 | **Sprint 3 rung-classifier decisions D-301…D-306 signed off** (offline pure modules proceeding; cloud build gated, Rule 5). **D-301 → v1 stress = `has_default_note` + `burden_ratio>0.30` + worsening-terms** (no servicing/bank feed today — confirmed); NSF/returned-payment is a **deferred signal** added when a feed lands → **FU-301** (Framework to absorb it). **D-302 → `rapid_reup_flag` owned in `common/rung`** (nothing computes it upstream today). Definition: TRUE when a new same-merchant advance funds while a prior position is still `active` AND the prior's `est_paydown_pct` (S2 clock, evaluated at the new `funded_date`) **< 50%**, OR consecutive `funded_date`s **≤ 45 days** apart (calibratable `RAPID_REUP_MAX_GAP_DAYS`). Paydown-based is PRIMARY (more honest than raw days); day-gap is the fallback when prior paydown can't be computed. Richer concurrent-position detection (other funders' debits) comes from S7 statement parsing — B.4 staging. **D-303 → REVISED to trust `Type=Renewal` as a true renewal** (CLAUDE.md §2.5 — `Type` is reliable). The 503 `renewal_unlinkable` are a **data-linkage gap, NOT evidence the renewal didn't happen** → counted as real renewals (good starting data), flagged `renewal_chain_incomplete`, tracked + resolved forward (**FU-302**). **Supersedes my original "treat unlinkable as missing-signal" rec** — no merchant is demoted to Unclassified for a linking gap. **D-304 → separate point-in-time `gold.merchant_rung` (+`_current` view) + append-only `gold.merchant_event_log`** (mirrors the S2 clock pattern). **D-305 → v1 event log = classification + transition events, one wide append-only table** keyed `(merchant_id, event_type, event_ts)`; S4/S5/S8 append their event types later. **D-306 → confidence = borderline-driven, deterministic.** Low confidence ⇒ values sit near a rung threshold; **missing data does NOT lower confidence** — extend benefit of the doubt, treat the merchant as good and keep advisory comms flowing until a signal says otherwise. Missing *key* signals still route to Unclassified / `missing_signals[]`, but a *classified* merchant is never penalized for absent peripheral data. Approach documented in SPRINT_3_PLAN §8. | Classifies the whole book honestly on the signals we actually have (Salesforce + S2 clock), trusts the one field CLAUDE.md says is reliable (`Type`), defers feed/statement-only signals (NSF, concurrent-position parsing) to S7 with a documented upgrade path, and bakes the "innocent-until-proven-otherwise" stance into confidence so we never punish merchants for our own data gaps. |
| C-016 | 2026-05-31 | **Sprint 2 clock decisions D-201…D-205 signed off** (build proceeding). **D-201 → separate point-in-time tables:** `gold.deal_clock` + `gold.merchant_clock`, partitioned by `clock_run_date`, append-only (idempotent per run via Delta `replaceWhere`), each with a `*_current` view = latest `clock_run_date`. Daily recompute never overwrites prior runs (auditable; clean lineage from `gold.deals`). **D-202 → true-split OFF by default:** fixed-ACH is the confirmed common case (Appendix A.6); `holdback_pct` stays deferred; revenue-split path built only if a later audit finds split deals. **D-203 → no servicing/bank feed today → estimated path for the whole book:** `balance_source='estimated'` everywhere; the `actual` path is wired (optional `servicing_feed` arg coalescing real payment counts) but inert until a feed lands. **D-204 → plain M–F (no holidays) for v1:** the daily business-day count excludes weekends only; a `holidays` parameter is threaded through `calendar.py` (default empty) so the US-Federal-holiday upgrade is a one-line constant change. **D-205 → 55% default renewal threshold:** reuse the existing `Thresholds.DEFAULT_RENEWAL_PAYDOWN=0.55` (Appendix A.4) for the whole book; a documented per-funder lookup hook in the transform is left null until the `mca_funders` threshold field is identified (no duplicate 0.55 constant — Rule 3). | Lets the THE-core-principle sprint (daily live-recompute) build now without inventing data we don't have (no feed, no per-funder threshold), while the point-in-time table shape preserves every day's recompute for audit. Estimated-path-only + softer advisory tone is honest about confidence (Framework). |
| C-015 | 2026-05-31 | **Companion docs refreshed with the agentic layer** (user-provided update in `docs/files_new/`, promoted into `docs/` root). Three files changed and were copied over: **CLAUDE.md** (adds §3 "Agentic layer" paragraph + renumbers late sprints), **Build Plan & Sprint Roadmap.docx** (new Section 7 "The Agentic Layer"; splits old S7 → **S7 agentic extraction** / **S8 advisory comms + agents + compliance**; app → **S9+**), **Framework.docx** (new Section 5.9 "The Agentic Layer"). The **Gold-Table Data Contract.xlsx** and **SPRINT_0.md** are byte-identical (md5 unchanged) → no-ops. **`SPRINT_TRACKER.md` at-a-glance table re-numbered** to match. **Key constraint added:** agents **articulate / extract / orchestrate** only — they **NEVER compute the spine** (clock, classifier, burden/eligibility, compliance, models stay deterministic + auditable); **no agents before the spine exists** (S7+). **Verified:** Appendices A (clock) and B (rung rules) and the Data Contract are unchanged → **S0–S6 design is unaffected**; agentic content lands in S7/S8. | Keeps the authoritative companion docs and the living tracker in sync (Rule 7); the change is additive (later-sprint scope), so the in-flight spine work (S1 done, S2 next) needs no rework. |
| C-014 | 2026-05-31 | **MRI mints its own `merchant_id` AND carries AATM's `azure_merchant_id` (DM Merchant Id) as a cross-system join field** (your call: "create your own id but maintain a field with Azure Merchant ID … so we have an Id that joins everything together"). **Investigation (profiled live):** there is **no direct id-to-id join** between the SF funded book and AATM — `Opportunity.Application_Submission__c` is a Salesforce id (`a0o5e0…`, only 17.8% populated) and is NOT AATM's `app_id` (`MERGE:291920` / `OM_2510488` / `HIST-…`; different id systems). The real bridge is **normalized tax_id**: 96.7% populated on MRI funded accounts (2,095/2,167), and **84% of MRI funded tax_ids (1,730/2,053) are present in AATM's `merchants` registry**. **Design:** mint MRI's own stable `merchant_id` (persisted crosswalk, D-101); populate `gold.merchants.azure_merchant_id` via a **build-time, read-only** join MRI-merchant-tax_id → `lakebase_aatm_prod.public.merchants.azure_merchant_id` (~84% fill; null + `azure_merchant_id_is_missing` flag otherwise). The enrichment is isolated/optional — MRI identity never depends on AATM at runtime; the column simply degrades to null if AATM is unavailable. Tie-break when a tax_id maps to >1 AATM merchant: deterministic (min `azure_merchant_id`). | Owns a stable join key (no coupling, full funded-book coverage) while still giving the company one column that reconciles MRI ↔ AATM ↔ leads. tax_id is the only honest cross-system key and it covers 84%. | 
| C-013 | 2026-05-31 | **AATM matching IP found + PORT chosen (resolves D-105).** The entity-resolution IP is the AATM `merchant_sync` job: workspace `/Users/venkat@morgancash.com/aatm/prod/files/jobs/lib/aatm_jobs` (foreign catalogs `lakebase_aatm_dev`/`_prod` over Lakebase Postgres hold its *output* — `merchants` + `merchant_lead_history`). The reusable IP is (a) pure normalizers `normalize_tax_id` / `normalize_business_name` (iterative suffix-fold LLC/Inc/Corp/Ltd/Co/…) / `normalize_phone`, and (b) `resolve_merchant`'s **4-tier priority chain** `azure_id → tax_id → name+phone → name+email`. **Decision: PORT the logic into `common/identity` (not call the service)** — AATM is row-by-row upserts against mutable Postgres keyed on an external `azure_merchant_id`; MRI needs a batch Spark dedup of `bronze.account` with no such external key. Ported normalizers verbatim (+added `normalize_state`, dropped `normalize_email`); ported AATM's `test_normalize.py` cases (**37 new tier-1 tests green**). MRI tier mapping: `MasterRecordId chains → exact Tax ID` AUTO-MERGE (D-102); `phone`, `name+state` → flagged candidates, not auto-merged v1. | Testability + lineage (own, versioned, tested code) over a cross-system call; honors Rule 6 (reuse proven IP) without coupling MRI to AATM's Postgres runtime. |
| C-012 | 2026-05-31 | **Field-level source-of-truth locked (closes D-002).** Resolved by profiling landed bronze (funded book = 3,959 `Opportunity` rows where `StageName='Funded'`). Deal economics come from the **selected offer** (`Offer__c` where `Select_Offer__c=true`, joined on `Opportunity__c=Opportunity.Id`): `factor_rate`←`Factor_Rate__c`, `payback_amount`←`Payback_Amount__c`, `payment_amount`←`Payment_Amount__c` (the **underscored** fields; the non-underscored `PaybackAmount__c`/`PaymentAmnt__c` are near-empty feed copies). `funded_amount`←`Opportunity.Funded_Amount__c` (anchor). Merchant link = `Opportunity.AccountId` (not a custom `Merchant__c`). `fico`←`Opportunity.FICO__c` (string; `Fico_Score__c` is 0%). `position`←`Positions__c` is 0% → blank=unknown. **DQ rule:** ~28 funded deals have >1 selected offer → silver dedups on latest `Offer__c.LastModifiedDate`; 2 funded deals have none → fall back to Opportunity fields + flag `selected_offer_missing`. `field_maps.DEALS_MAP` updated to real API names; 32 tier-1 tests green. | Decided on real values, not guesses; honors the no-surface / distrust-SF-balances rules. |
| C-011 | 2026-05-31 | **Offer object API name = `Offer__c`** (resolves the object-level half of D-002). The "Selected Offer" is not a separate object — it's the row of `Offer__c` where `Select_Offer__c = true`. A single `Offer__c` bronze table therefore serves both the full offers list and the selected-offer economics. Excluded `Offer__Feed` (Chatter) and `Offer__History` (Offer-object field history; our event source is `OpportunityFieldHistory`). | Confirmed live in the Lakeflow ingestion wizard against the `mri_salesforce` connection. `constants.SFObject.OFFER` already = `"Offer__c"`. |
| C-008 | 2026-05-29 | **Root `CLAUDE.md` pointer** (D-003 → option b): keep one source in `docs/`, add a root pointer so Claude Code auto-loads it | One source of truth, still loads from repo root |
| C-007 | 2026-05-29 | **Date-sanity rule** (D-001 → option b/c): flag a large gap in *either* direction, not just `funded > created` | Catches the real migration artifact (Funded 2020 / Created 2022) the spec describes |
| C-010 | 2026-05-29 | **All-managed tables** for `mca_mri` bronze/silver/gold (Option B). Verified Lakeflow Connect destinations are managed streaming tables (external not selectable); chose managed end-to-end for the simplest fully-native setup. Diverges from the house external convention (`mca_funders`/`mca_leads` use external Delta at `abfss://mca-pipeline@dlsmcdatastoreprd…`). | No material cost difference (identical storage $/GB; managed adds minor auto-optimization compute, negligible at this data size). Native Lakeflow path; honors CLAUDE.md §4. Revisit if cross-engine reads or data-residency policy emerge. |
| C-009 | 2026-05-29 | **No separate secret scope** for Salesforce creds (supersedes C-006/D-005). The interactive OAuth flow stores credentials inside the **UC connection** `mri_salesforce` itself (browser → UC, governed). | Fewer moving parts; secret never transits chat/code/shell. Scope only needed if we later move to headless JWT |
| C-006 | 2026-05-29 | ~~Secret backend = Databricks-backed scope `mri-salesforce`~~ — **superseded by C-009** | UC connection holds creds; no scope required for OAuth web flow |
| C-005 | 2026-05-29 | **Salesforce OAuth grant type** = web / authorization-code behind the existing integration user (D-004 → default a) | Matches Databricks "Add connection" wizard; JWT later if needed |
| C-004 | 2026-05-29 | Scaffold repo + write plans now; **no cloud resources** (catalog/connection/pipeline) until separately approved | Lets us build & test offline; honors Rule 5 deploy gate |
| C-003 | 2026-05-29 | Guide the user through creating a Salesforce **Connected App (OAuth)** for Lakeflow Connect | Native, best-practice ingestion path; user did not yet have a credential chosen |
| C-002 | 2026-05-29 | New Unity Catalog catalog = **`mca_mri`** (schemas: `bronze`/`silver`/`gold` + `_test` mirrors) | Matches house family (`mca_funders`, `mca_leads`); supersedes the literal `mri` name in CLAUDE.md/specs — schema names unchanged |
| C-001 | 2026-05-29 | Data rights confirmed: Morgan Cash holds full rights to use the funded-book data this way (G4 legal gate) | Stated by Venkat |

## Open — awaiting your decision

**Sprint 1 (Identity + Deal Table) — sign-off needed before build** (detail in [SPRINT_1_PLAN](sprints/SPRINT_1_PLAN.md) §8):

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-101 | 2026-05-31 | **`merchant_id` minting & stability** — persisted crosswalk vs deterministic hash. | Persisted crosswalk (ids never re-key on re-merge → stable downstream join). |
| D-102 | 2026-05-31 | **Match confidence / auto-merge policy** across Tax ID / phone / name+state. | Exact Tax ID → auto-merge; weaker keys → flagged candidates, not auto-merged in v1. |
| D-103 | 2026-05-31 | **`is_renewal_of` definition** — how renewal chains are linked. | Per-merchant order by `funded_date`; link Renewal/Buyout to prior same-merchant deal; validate vs any SF link field. |
| D-104 | 2026-05-31 | **Layer placement** of Deal Table + merchant dimension — gold vs silver. | Gold (`gold.deals`, `gold.merchants`) — conformed single source of truth for S2+. |

**D-101…D-104 approved 2026-05-31** ("Approve all recommendations") — recommendations adopted as written; build proceeding. **D-105 resolved — see C-013.** (D-002 resolved — see C-011 + C-012.)

**Sprint 2 (Feature Layer + Amortization Clock) — sign-off needed before build** (detail in [SPRINT_2_PLAN](sprints/SPRINT_2_PLAN.md) §8):

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-201 | 2026-05-31 | **Clock output shape/placement** — separate point-in-time tables vs append columns in place. | Separate `gold.deal_clock` + `gold.merchant_clock` keyed `(id, clock_run_date)`, append-only, with a `current` view (auditable daily recompute; no destructive overwrite). |
| D-202 | 2026-05-31 | **True-split / holdback handling** (A.6). | Off by default (fixed-ACH is the confirmed common case); build the estimated-via-revenue split path only if the audit finds split deals. |
| D-203 | 2026-05-31 | **Servicing/bank feed availability.** | Confirm no feed today → v1 runs the estimated path for the whole book; `actual` path wired but inert until a feed lands. |
| D-204 | 2026-05-31 | **Business-day calendar / holidays** for the daily count. | Single US Federal holiday set (M–F minus holidays); confirm source or whether plain M–F is acceptable for v1. |
| D-205 | 2026-05-31 | **Funder renewal-threshold lookup** in `mca_funders`. | Use per-funder threshold where present; default 55% until wired. Need dataset pointer + field. |

**D-201…D-205 approved 2026-05-31** — see **C-016**. D-201 separate point-in-time clock tables; D-202 true-split off by default; D-203 estimated-path-only (no feed today); D-204 plain M–F for v1 (holiday hook threaded); D-205 55% default (reuse `DEFAULT_RENEWAL_PAYDOWN`), per-funder lookup hook null until the `mca_funders` field is identified. **Open follow-up FU-201:** identify the per-funder renewal-threshold field in `mca_funders` to populate the lookup (non-blocking).

**Sprint 3 (Rung Classifier + State Machine + Event Log, Appendix B) — sign-off needed before build** (detail in [SPRINT_3_PLAN](sprints/SPRINT_3_PLAN.md) §8):

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-301 | 2026-05-31 | **Stress-event source for Distressed** — is NSF/bounced-payment available, or is v1 stress = default-note + burden + worsening-terms only? | v1 stress = `has_default_note` + `burden_ratio>0.30` + worsening-terms; add NSF when a servicing feed / S7 extraction lands. |
| D-302 | 2026-05-31 | **`rapid_reup_flag` ownership** (Rule 3) — PRIMARY Serial signal derived upstream (S1/S2) and read here, or computed in S3? | Define once: if it's a clock/identity property own it upstream and read it; else own it in `common/rung`. No duplicate. |
| D-303 | 2026-05-31 | **"≥1 prior clean renewal" + the 503 `renewal_unlinkable`** — does Disciplined rely on S1's linked chain, and how are unlinkable renewals treated? | Linked-chain-based; unlinkable → missing-signal (never assert Disciplined on incomplete history; counts Unclassified-leaning). |
| D-304 | 2026-05-31 | **Rung output shape/placement.** | Separate point-in-time `gold.merchant_rung` keyed `(merchant_id, classify_run_date)`, append-only + `merchant_rung_current` view; append-only `gold.merchant_event_log` (mirrors the S2 clock pattern). |
| D-305 | 2026-05-31 | **Event-log scope for v1** — which events, one wide table vs typed per family? | v1 emits classification + transition events (queue + Phase-6 learning spine); touch/comms/offer events added by S4/S5/S8 writing to the same log. |
| D-306 | 2026-05-31 | **Confidence scoring definition** — deterministic rules score, formula approach? | Deterministic completeness score (fraction-of-required-signals-present, penalized by borderline thresholds), NOT an ML probability; drives queue ordering. Confirm formula before lock. |

**D-301…D-306 approved 2026-06-01** — see **C-017**. D-301 v1 stress = default-note + burden + terms (NSF deferred); D-302 `rapid_reup_flag` in `common/rung` (paydown<50% on still-active prior, OR ≤45-day gap); D-303 trust `Type=Renewal`, unlinkable = data gap to track (not a disqualifier); D-304 separate point-in-time `gold.merchant_rung` + append-only `gold.merchant_event_log`; D-305 one wide event log (classification + transition v1); D-306 borderline-driven confidence, missing data never penalized. **New non-blocking follow-ups: FU-301** (add NSF + statement-derived signals — concurrent positions, returns, real revenue — to the Framework when a servicing feed / S7 Statement Analyst lands); **FU-302** (resolve the 503 `renewal_chain_incomplete` links forward). S3 deliverable: rung + lifecycle state + daily queue (no ML); exit = whole book classified, Unclassified pile quantified, reconciles.

**S3 build + tier-2 on `gold_test` DONE 2026-06-02 (D-301…D-306 implemented literally, no new decisions arose):** `common/rung/{lifecycle,waterfall,confidence,classify}.py` + `common/eventlog/events.py` (all pure) + `transform/gold_rung.py` (Spark) + `schemas/gold.merchant_rung_schema()`/`event_log_schema()` + `field_maps.MERCHANT_RUNG_MAP`/event-log columns + `constants` enums + `RAPID_REUP_MAX_GAP_DAYS=45` (reused existing `Thresholds` — no duplicate numbers). **52 new tier-1 → 217 tier-1 green.** **Tier-2 recon PASSED on `gold_test` (`failures: []`):** 2,125/2,125 merchants classified (key unique); enum/bounds/gated-null-rung/Unclassified-bucket/no-surface/event-keying all green; two-run state machine append-only + `_current` advance verified. Diagnostics: dormant 1,458 / active 422 / new-establishing 239 / defaulted 6; rungs Serial 169 / Distressed 65 / Disciplined 47 / **Unclassified 141 (quantified, top missing = disclosed_positions_cnt, est_weekly_revenue, burden_ratio, est_paydown_pct)**; no Growth/Graduate (burden null v1 — honest); rapid_reup 287; renewal_chain_incomplete 503 (= S1 unlinkable); Wolf → Serial (B.5 match). **PROD `gold` promotion DONE 2026-06-02 (approved):** re-ran with `schema=gold, allow_prod=True, second_run=False` (clean single partition) — recon identical (`failures: []`). PROD: `mca_mri.gold.merchant_rung` (2,125) + `merchant_rung_current` (view) + `merchant_event_log` (2,125 events). **S3 COMPLETE.** Detail in SPRINT_TRACKER "Sprint 3 — progress detail".

**S2 clock built + tier-2 verified on `gold_test` 2026-05-31 (`failures: []`).** `common/clock/*` + `transform/gold_clock.py` + clock field maps/schemas; **165 tier-1 green** (+59 clock). Tier-2 recon (run_date 2026-05-31): `deal_clock`=3,959 (= gold.deals, key unique), `merchant_clock`=2,125 (= distinct merchants); paydown∈[0,1], balance≥0, closure 3-state, **0 default-note-but-closed_clean** (Starr invariant), `balance_source` 100% estimated, revenue/burden 100% null+flagged, `*_current` = latest run_date. Diagnostics: closed_clean 3,059 / active 894 / closed_default 6; clock_inputs_missing 234; rtr_checkpoint>tol 21 (terms-contradiction flags). **PROD `gold` promotion DONE 2026-05-31 (approved):** re-ran with `schema=gold, allow_prod=true` — recon identical (`failures: []`). PROD: `mca_mri.gold.{deal_clock, merchant_clock}` (managed) + `{deal_clock_current, merchant_clock_current}` (views). New non-blocking follow-up **FU-202:** the 21 `rtr_checkpoint_delta` > tolerance deals (max 372,500) are terms contradictions (funded×factor vs total_payback) worth a data review.

**Sprint 4 (Activation to Salesforce + Book Health) — sign-off needed before build** (detail in [SPRINT_4_PLAN](sprints/SPRINT_4_PLAN.md) §8):

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-401 | 2026-06-02 | **State machine definitions + thresholds** — `current_state` ∈ {clock-running, approaching, in-market, renewed, lost-winback} + transition rules. | `in-market` = active & `is_eligible_now`; `approaching` = active & eligible within `APPROACHING_WINDOW_DAYS` (**30**, calibratable) of `est_renewal_eligible_date`; `clock-running` = active, earlier; `renewed` = new advance funded recently; `lost-winback` = dormant/defaulted. Sign off states + 30-day window. |
| D-402 | 2026-06-02 | **Named plays catalogue + SLA + `play_owner` source.** | Deterministic (rung × state) → play matrix; SLA constants (sliding/distressed **2** business days, in-market **5**, else **10**); grounded next-action templates; `play_owner` ← SF Opportunity OwnerId (ingest) else null + `play_owner_is_missing` (FU-101). Sign off plays + SLAs + owner source. |
| D-403 | 2026-06-02 | **Reverse-ETL mechanism + Salesforce target (headline).** | Build the activation gold table + SF write-back field map + Lakebase serving mirror; write ONLY to dedicated MRI fields on the chosen SF object (recommend a small set on `Opportunity` or a custom `MRI__c` block — needs SF-side fields + a sandbox); **prod SF write gated on `allow_sf_write=True` + approval** (mirrors `allow_prod`). Confirm SF target + whether S4 writes to SF or stops at the Lakebase/gold serving layer for v1. |
| D-404 | 2026-06-02 | **Book Health output shape + v1 metric scope.** | Separate point-in-time `gold.book_health_*` (+`_current`) keyed `report_date`; v1 = rung distribution + drift, default/restructure trend, sliding count, approaching pipeline, concentration risk, partial renewal-capture; **defer** LTV / defection / offer-acceptance / value-to-ask / comms-SLA to S5/S6/S8 (inputs not built). Sign off shape + split. |
| D-405 | 2026-06-02 | **Scope boundary** — the Build Plan listed the state machine + plays + SLA under **S3**; the signed S3 plan (C-017) deferred activation to S4. | **S4 absorbs the state machine + plays + SLA + next actions.** Confirm. |
| D-406 | 2026-06-02 | **Daily queue surface.** | `gold.daily_queue` **view** over `merchant_activation_current` + clock, ordered sliding-first → in-market/approaching → `play_sla_due` → confidence (carried from S3 plan §1). Confirm. |

**D-401…D-406 approved 2026-06-02 — see C-018.** D-403 resolved to the **serving-layer-only** path (no Salesforce write in S4); the governed SF write-back is deferred to **FU-401** (its own gated step). **S4 BUILT + tier-2 PASSED on `gold_test` then PROMOTED to PROD `gold` 2026-06-02 (`failures: []`):** `common/activation` + `common/bookhealth` (pure) + `transform/gold_activation.py` + `transform/gold_book_health.py`; 2,125/2,125 merchants activated; `daily_queue` + Book Health reconcile; two-run state machine append integrity; Wolf → serial-renewal-vs-buyout (B.5). **35 new tier-1 → 251 green.** PROD: `gold.merchant_activation` (+`_current`), `gold.daily_queue`, `gold.book_health` (+3 `_current` views). **New non-blocking follow-up FU-401:** governed Salesforce write-back to dedicated `MRI__*` fields (sandbox-first, `allow_sf_write`-gated) + Lakebase serving sync. **S4 COMPLETE.**

**Sprint 5 (Offer Engine — Proactive) — sign-off needed before build** (detail in [SPRINT_5_PLAN](sprints/SPRINT_5_PLAN.md) §8). Existing IP located: funder-criteria dataset + routing engine = the **`mca_funders`** catalog (silver boxes `funder_programs`/`funder_program_versions`/`funder_industries`/`funder_states`/`funder_operations`; gold routing outputs `routing_program_evaluations`/`routing_decisions`; input contract `gold.v_funder_input`) + the versioned engine code in workspace `mca-funder-2A-3` / Azure DevOps `DataManagement`.

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-501 | 2026-06-02 | **Routing-engine reuse mechanism** (headline; mirrors C-013). Invoke vs port vs reuse-existing-evaluations. | **Reuse, do NOT port** (versioned/complex boxes — porting = forbidden rebuild). Prefer **invoke** the engine on MRI-derived profiles on a cadence → read back `routing_program_evaluations`; fallback = reuse existing evaluations joined by merchant id (D-502); thin criteria-match last resort (flagged partial). Needs a short spike to confirm the batch entrypoint. |
| D-502 | 2026-06-02 | **MRI ↔ `mca_funders` merchant identity join** so evaluations map back. | Join on `gold.merchants.azure_merchant_id` → `mca_funders` merchant id (else normalized tax_id); unmatched → `none-yet` + flag. Confirm key + overlap via spike. |
| D-503 | 2026-06-02 | **Merchant profile assembly** (`v_funder_input` shape) + missing fields. | Populate what MRI has (FICO, TIB, active_position_cnt, governing_state, industry); leave revenue/NSF/bankruptcy absent → rely on the engine's missing-data verdicts; never fake. |
| D-504 | 2026-06-02 | **`eligible_offer_types` derivation.** | renewal (in-market single-position disciplined) / buyout+larger-advance (serial/rapid-reup & a box passes) / none-yet (not eligible, gated, or no match). |
| D-505 | 2026-06-02 | **`max_sustainable_advance`** (revenue-dependent; revenue null v1). | null + `_is_missing` v1 (hook wired, FU-301), not a fabricated ceiling (vs an RTR-headroom proxy). |
| D-506 | 2026-06-02 | **Renewal-vs-buyout suitability gate (v1).** | Build it — compute both structures + the double-dip delta (S2 clock); recommend renewal / buyout / wait-and-pay-down, merchant's interest the tiebreaker (serves Wolf). |
| D-507 | 2026-06-02 | **Offer output shape/placement.** | Separate point-in-time `gold.merchant_offers` keyed `(merchant_id, offer_run_date)`, append-only + `_current` view (mirrors S2/S3/S4). |
| D-508 | 2026-06-02 | **Compliance-gate scope in S5.** | Interface/hook only (`offer_vs_advice` placeholder); the full compliance block is S8. |

**D-501…D-508 approved 2026-06-02 — see C-019.** D-501 resolved to the **batch handoff** path (routing team evaluates MRI-supplied profiles; MRI reads results back) — NOT invoke, NOT reuse-existing (0 id-overlap, D-502). Offline `common/offer` built + **270 tier-1 green**. **New follow-up FU-501:** define the profile-handoff contract with the routing team (landing table + MRI-`merchant_id` round-trip + cadence). The MRI-side cloud build (`transform/gold_offers.py` = profile export + results consumer) is **gated on FU-501** (the handoff contract) + approval (Rule 5).

**Sprint 6 (Prediction Models) — sign-off needed before build** (detail in [SPRINT_6_PLAN](sprints/SPRINT_6_PLAN.md) §8). First ML sprint — ADOPT PyMC-Marketing (BG/NBD+Gamma-Gamma+CLV) + lifelines (Cox+KM), not hand-built (CLAUDE.md §4); toolkit chosen for the sparse-data case (Framework §11.2: BTYD from minimal data, Cox censoring). Reads S1–S4 gold + event log; distress stays signal-driven.

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-601 | 2026-06-02 | **RFM feature definitions + grain.** | One row/merchant; event = a funded advance; frequency = deal_count−1; recency = last−first; T = today−first; monetary = avg funded_amount. |
| D-602 | 2026-06-02 | **Model libraries + runtime.** | Adopt PyMC-Marketing + lifelines + MLflow on a Databricks ML runtime; add to requirements; fitting runs on Databricks (not tier-1 local). |
| D-603 | 2026-06-02 | **Sparse-data handling + confidence.** | Hierarchical/book-level priors; Cox censoring for not-yet-renewed; explicit `insufficient_history` flag (repeat events < `INSUFFICIENT_HISTORY_MIN_EVENTS`=1); `prediction_confidence` = normalized posterior width. |
| D-604 | 2026-06-02 | **Output shape/placement.** | Separate point-in-time `gold.merchant_predictions` keyed `(merchant_id, prediction_run_date)`, append-only + `_current` (mirrors S2–S5). |
| D-605 | 2026-06-02 | **MLflow + serving scope.** | MLflow tracking + registry + batch inference to gold + `prediction` events; defer the real-time Model Serving endpoint (batch-only v1). |
| D-606 | 2026-06-02 | **CLV horizon + discount rate.** | future_t = 12 months; discount_rate ≈ 12% annual (both calibratable constants). |
| D-607 | 2026-06-02 | **Cox covariates + event def.** | event = took capital vs censored; duration = interval-to-next (else tenure censor); covariates = factor_trend, active_position_cnt, payment_health, industry_vertical; **drop burden_ratio** (null book-wide v1, FU-301). |
| D-608 | 2026-06-02 | **Backtest method + acceptance.** | Time-split train/holdout; predicted-vs-actual next-event calibration + ordering; KM baseline per rung; accept on "sane + explainable", not a fixed accuracy bar. |
| D-609 | 2026-06-02 | **Prediction event + queue integration.** | Emit `prediction` events; S4 queue refines ordering by p_defection + predicted_next_event_date (never overrides the deterministic gates). |

**D-601…D-609 approved 2026-06-02 — see C-020.** Data-readiness spike done (796 repeat / 1,329 single-deal). **D-602 adjusted at build time:** v1 BG/NBD+Gamma-Gamma+CLV ships on the lightweight **`lifetimes`** lib (pure numpy/scipy — PyMC-Marketing's native PyTensor stack crashed the cluster driver across 4 attempts; **FU-602** tracks the PyMC Bayesian upgrade once its env is sorted interactively) + **lifelines** Cox/KM, on a 14.3 LTS ML single-node cluster. **Tier-2 PASSED on `gold_test` (`failures: []`):** 2,125 merchants predicted (coverage/keys/bounds/honesty green), insufficient_history 1,329, CLV non-null 796, Cox fitted; calibration note on the p_alive-by-lifecycle pattern (refine with FU-602/FU-301). **PROD `gold` gated** (Rule 5). **Follow-ups: FU-601 RESOLVED in code 2026-06-02** (centralized `DealType.REPEAT_TYPES`={Renewal,Buyout,Stack,Add-On}=all non-New; fixed S1 `gold_deals` renewal chain + S3 `gold_rung` + S4 `gold_activation` `has_renewal`; +1 tier-1 → 285; transform re-run to propagate to live tables is gated); **FU-602** (PyMC-Marketing Bayesian upgrade + posterior-width confidence).

**Sprint 7 (Agentic Extraction — Statement Analyst + Data Steward) — sign-off needed before build** (detail in [SPRINT_7_PLAN](sprints/SPRINT_7_PLAN.md) §8). First agentic sprint (Framework §5.9): agents **extract**, the spine still **computes**; agents are grounded + logged + call deterministic tools + never recompute the spine.

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-701 | 2026-06-05 | **LLM / agent platform.** | Databricks-native: Foundation Model APIs (Claude/Llama endpoint) + Mosaic AI Agent Framework (governance/lineage/audit); batch; LLM spend gated. Confirm platform + model + cost. |
| D-702 | 2026-06-05 | **Bank-statement source + ingestion** (NOT ingested in S0). | Confirm PDFs live in SF ContentVersion/Attachments; stand up a read-only ingestion path to UC; spike availability/volume/format. **Gates the Statement Analyst (Phase 2).** |
| D-703 | 2026-06-05 | **Phasing.** | **Data Steward first** (self-contained on existing `silver.deals.notes`; resolves `default_subtype`, validates vs Starr; no OCR/PDF infra); **Statement Analyst second** (after D-702 + a labeled sample). |
| D-704 | 2026-06-05 | **Extraction output shape.** | Separate point-in-time `gold.merchant_extraction` (+`_current`) with source_ref/confidence/model_version/review_status; the spine consumes it as optional enrichment via the normal re-run — the agent never writes spine tables. |
| D-705 | 2026-06-05 | **Guardrails / grounding / human-review.** | Every extraction grounded (source_ref + citation + confidence); `< AGENT_CONFIDENCE_REVIEW_MIN` → human-review queue, never auto-applied; agents call deterministic tools for all counting/classification; full event-log audit incl. model_version. Confirm the threshold. |
| D-706 | 2026-06-05 | **Accuracy bar + labeled sample.** | Hand-label a sample (the four merchants + N more) for default sub-type + statement positions; "sane + improving" acceptance (sub-type accuracy ≥ X%; positions within ±1), calibrated on the first sample. Confirm the approach. |

**D-701…D-706 approved 2026-06-05 — see C-022.** **DONE → in PROD `gold` (C-023 run, C-024 D-706 PASS 6/6).** Phase 1 (Data Steward) shipped: offline deterministic foundation → LLM agent (Foundation Models) → `gold_test` run → D-706 labeled-sample accuracy gate → PROD promotion. Phase 2 (Statement Analyst) deferred — statements confirmed in SF (D-702 ingestion spike at Phase 2 start).

**Sprint 7 Phase 2 (Statement Analyst) — sign-off needed before any build/cloud run** (detail in [D-702 spike + Phase-2 plan](D-702_statement_ingestion_spike.md)). The **D-702 statement-ingestion spike (desk)** is complete (2026-06-06): confirmed S0 ingested **no** file/attachment object (statements are outside the lakehouse); candidate SF stores are **ContentVersion/ContentDocumentLink** (most likely) and/or legacy **Attachment**; scoped a bronze→silver→gold ingestion path (Lakeflow ingests **metadata**, a gated read-only fetch lands **binaries** in a UC Volume, deterministic silver **OCR**, then the agent extracts to `gold.merchant_extraction` — **no extraction-schema change needed**, the types/`value` already exist); and designed the labeled sample mirroring D-706. **Open finding (honest):** the *actual* object, per-deal **coverage %**, file-type mix, volume, and the "is-a-bank-statement" identification rule require a **gated read-only cloud probe (NOT run, Rule 5)** — scoped in §1.4, and the first action on approval.

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-707 | 2026-06-06 | **Statement source object + binary-retrieval mechanism.** | Lakeflow ingests `ContentVersion`/`ContentDocumentLink` (+`Attachment` if present) **metadata** to bronze; a **separate gated read-only fetch** downloads candidate PDFs to a UC **Volume** + `bronze.statement_files`. First action: run the §1.4 read-only probe to confirm object/coverage/file-types/volume/identification rule. |
| D-708 | 2026-06-06 | **OCR/parse placement** — deterministic silver vs agent-native multimodal. | **Deterministic silver OCR** (`ai_parse_document`/Document AI for scans; pdfplumber for digital) → `silver.statement_text`; agent classifies from text. Keeps the agent pure judgment; OCR cacheable/auditable/cheaper on re-run. |
| D-709 | 2026-06-06 | **Statement scope / recency.** | **Funding-moment statements only** (application-bundle months at/just-before `funded_date`; most-recent ~3 if multiple). NOT live (that's the app, S9+). |
| D-710 | 2026-06-06 | **Statement→spine enrichment integration** (no agent-written spine column). | Clock reads **APPLIED** `concurrent_positions`/`weekly_debit`/`est_weekly_revenue` from `merchant_extraction_current` as **optional enrichment**: other-funder positions **ADD** to `active_position_cnt`; deposits fill `est_weekly_revenue` → real `burden_ratio`; absent/low-confidence ⇒ today's behavior. Confirm the merge rule (add vs override). Advances FU-301. |
| D-711 | 2026-06-06 | **Labeled sample composition + accuracy bars.** | Hand-label ~6–10 deals incl. the four merchants (**Wolf** central); **positions ±1**, **weekly_debit/revenue ±10%** (calibrate on sample 1), overall **≥ 0.80**; permanent tier-2 gate + regression guards (no under-count of a present position; abstain not fabricate). Mirrors D-706. |
| D-712 | 2026-06-06 | **Bank-statement PII / no-surface** (more sensitive than Notes). | Statements + OCR text stay in governed UC (restricted Volume + silver); define a **no-surface guard** for raw account numbers / running balances (analogous to `_sf_stored_*`); only derived aggregates leave. Per Data Contract PII/REG codes; full compliance gate is S8. |
| D-713 | 2026-06-06 | **OCR + LLM cost / batch scope.** | **Sample-first** → measure accuracy → batch the deals that have statements; OCR cached in silver (no re-OCR); cost-confirmed + spend-gated (Rule 5). |

**Sprint 8 (Advisory Layer — Advisory Composer + Structure Advisor + Compliance Gate) — sign-off needed before build** (detail in [SPRINT_8_PLAN](sprints/SPRINT_8_PLAN.md) §8). Turns the spine's computed facts (rung, clock, offers, extractions, predictions) into **honest, grounded, merchant-facing advisory guidance**, with a **first-class deterministic compliance gate** every merchant-facing output must pass (Framework §2.3 honesty / §2.4 compliance-is-real / §5.9 agents-articulate-never-compute). **Realizes the S5 `compliance_gate_hook` (D-508).** This first pass builds **OFFLINE deterministic modules + the pure agent layer only** (the S7 pattern); the FM/cloud transform + any delivery are designed but **gated** (Rule 5). **S8 composes + gates; it does NOT send.**

| ID | Opened | Question | Recommendation |
|---|---|---|---|
| D-801 | 2026-07-13 | **Compliance gate — placement, shape, hard-vs-soft.** | A pure, deterministic `common/compliance/` block (classify + disclosure + gate), tier-1 tested, realizing the S5 hook (D-508). A **HARD gate**: ungrounded / a suppressed-unsuitable offer pitched as specific / a missing required disclosure → **BLOCKED** (stored, never deliverable), not merely annotated. **Scope = merchant-facing outputs only** (S4 internal rep guidance untouched). |
| D-802 | 2026-07-13 | **Advisory Composer grounding contract.** | The Composer speaks **only** a deterministic **fact pack** (each number = value + source gold field + run_date); a grounding validator **rejects any numeric token not in the pack**; output is a **structured advisory record** (headline/rationale/recommended_action/grounded_refs), not free prose. Never computes the spine. Reuses `common/agents/grounding.py`. |
| D-803 | 2026-07-13 | **Structure Advisor scope.** | **Articulates only** the existing S5 `structure_evaluation`/`suitability_verdict` (renewal / buyout / wait-and-pay-down + the double-dip cost `common/offer/structure.py` already computes); **never recomputes** the math; **cannot un-suppress** a suppressed offer. Explains *why wait / why renew* honestly (serves Wolf). |
| D-804 | 2026-07-13 | **Advisory output shape/placement + review/gating.** | Separate point-in-time `gold.merchant_advisory` (+`_current`) keyed `(merchant_id, advisory_run_date)`, append-only, carrying grounded_refs / confidence / model_version / **compliance_status** / required_disclosures / review_status; **stored, not delivered** (no outbound in S8). Mirrors D-704/D-604/D-507. |
| D-805 | 2026-07-13 | **State-aware disclosure rule source + v1 scope** (legal-adjacent). | A config-driven `disclosure_rules` table keyed by `governing_state`, seeded with confirmable commercial-financing disclosure regimes (e.g. **CA**, **NY** CFDL, **UT**, **VA**, others as researched); **v1 flags which regime applies + requires a disclosure block, and does NOT draft binding legal language** (counsel owns wording). **Please confirm the state list + the "flag + require, don't draft" v1 line + whether counsel reviews before any cloud run.** |
| D-806 | 2026-07-13 | **Advice-vs-specific-offer classification rule** (the core §2.4 gate). | Deterministic `classify_output_type`: **specific-offer** = names concrete terms (amount/factor/payment); **advice** = general guidance/eligibility/paydown coaching, no concrete terms; **factual-summary** = the merchant's own computed situation. Specific-offer → strict path (disclosure required + suitability must be SURFACE). **The agent labels intent; the deterministic gate enforces.** |
| D-807 | 2026-07-13 | **Advisory quality bar + labeled sample.** | Hand-label a sample (four merchants + N more) scored on **grounding (0 invented numbers — hard 100%)**, **compliance-gate correctness** (output-type + disclosure vs labels), and **honesty** (recommends *wait / don't take money* where the math says so). Permanent tier-2 gate + regression guards (never pitch a suppressed double-dip; never emit an ungrounded number), calibrated on sample 1. Mirrors D-706/D-711. |

**D-801…D-807 approved 2026-07-18 — see C-031.** Recommendations adopted as written. Offline deterministic + pure-agent build proceeding (`common/compliance/` + `common/advisory/` + `agents/` + tier-1); the FM/cloud transform, `gold_test`/PROD runs, and any delivery remain individually gated (Rule 5).

## Resolved (was open)

| ID | Opened | Question | Resolution |
|---|---|---|---|
| D-002 | 2026-05-29 | **Salesforce object + field API names.** | **Resolved 2026-05-31.** Object-level via C-011 (Offer object = `Offer__c`; Merchant = `Account`; event source = `OpportunityFieldHistory`); field-level via C-012 (selected-offer economics, underscored duplicate fields, `AccountId` merchant link, `FICO__c` string, selected-offer dedup DQ rule). `field_maps.DEALS_MAP` locked to real API names. |

<details><summary>Original D-002 framing (for history)</summary>

| ID | Opened | Question | Options | Recommendation |
|---|---|---|---|---|
| D-002 | 2026-05-29 | **Salesforce object + field API names.** Object-level: **resolved** — Offer object = `Offer__c` (C-011); Merchant parent = `Account`; event source = `OpportunityFieldHistory`. Field-level: still open — `Offer__c` exposes **duplicate-looking pairs** (`PaybackAmount__c` vs `Payback_Amount__c`; `PaymentAmnt__c` vs `Payment_Amount__c`) and the deal-economics fields (`Factor_Rate__c`, `Funded_Amount__c`, `Number_Payments__c`, `Frequency__c`, `Total_House_Commission__c`, `Offer_Expiration_Date__c`, links `Opportunity__c`/`Merchant__c`). Must determine which of each pair is authoritative. | Profile landed bronze values to pick the authoritative field of each duplicate pair; then lock `field_maps.DEALS_MAP` source labels. | Resolve the field-level half **after bronze lands** (read real values), before building the bronze→silver transform. |

</details>
